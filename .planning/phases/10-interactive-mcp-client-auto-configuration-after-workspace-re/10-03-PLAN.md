---
phase: 10-interactive-mcp-client-auto-configuration-after-workspace-re
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/SETUP_AGENT.md
  - README.md
  - docs/reference-readme.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Every documented OpenCode MCP config example uses `\"type\": \"remote\"`, matching official OpenCode docs and the config this phase's code now generates (RESEARCH Pitfall 1)"
    - "Claude Code examples keep `\"type\": \"http\"` (correct for Claude Code — only the OpenCode blocks change)"
    - "The `?workspace=` guidance already present in the docs stays intact"
  artifacts:
    - "docs/SETUP_AGENT.md — OpenCode Step 9 example corrected to type:remote"
    - "README.md — MCP config example (OpenCode `mcp` block) corrected to type:remote"
    - "docs/reference-readme.md — MCP config example corrected to type:remote"
  key_links:
    - "docs OpenCode examples -> match the type:remote value written by writeOpenCodeMCPConfig (Plan 02) so a user reading docs is not misled"
---

<objective>
Fix the pre-existing documentation bug where OpenCode MCP config examples show `"type": "http"`, which is invalid per official OpenCode docs (they require `"type": "remote"` for HTTP/remote servers). Correct the three doc files that carry this example.

Purpose: RESEARCH.md Pitfall 1 confirms OpenCode's schema has no `"http"`/`"stdio"` naming — only `"remote"`/`"local"`. The stale `"http"` example would silently fail to connect and would directly contradict the correct value this phase's code now writes (Plan 02). Fixing it in the same phase keeps docs and generated config consistent. This is additive-correctness only: Claude Code's `"type": "http"` is correct and stays; only OpenCode blocks change (does not violate D-04, which protects the global-config recommendation, not a schema typo).
Output: Three corrected doc files; no code change.
</objective>

<execution_context>
@/Users/tamlh/workspaces/self/AI/Tools/nano-brain/.claude/gsd-core/workflows/execute-plan.md
@/Users/tamlh/workspaces/self/AI/Tools/nano-brain/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-interactive-mcp-client-auto-configuration-after-workspace-re/10-RESEARCH.md

@docs/SETUP_AGENT.md
@README.md
@docs/reference-readme.md
</context>

<artifacts_produced>
## Artifacts This Plan Produces

No new files, no code. Three edits, each changing exactly the OpenCode example's `type` value from `http` to `remote`:
- `docs/SETUP_AGENT.md` — the OpenCode block under Step 9 (around line 190-197, the `"mcp"` block; the Claude Code block above it at line 174-184 keeps `type:http`). Also update the trailing "Other MCP clients" note (line 200) if it implies OpenCode uses `http`.
- `README.md` — the MCP config example (around line 238-246, the `"mcp"` block) — this example is labeled generically ("Claude Code, OpenCode, Cursor, etc.") but uses the OpenCode-style `mcp` key with `type:http`; correct to `type:remote` since the `mcp` top-level key is OpenCode's schema.
- `docs/reference-readme.md` — the MCP config example (around line 166-174, the `"mcp"` block) — same correction.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Correct OpenCode type in SETUP_AGENT.md and reference-readme.md</name>
  <files>docs/SETUP_AGENT.md, docs/reference-readme.md</files>
  <read_first>
    - docs/SETUP_AGENT.md:169-208 (Step 9 — Claude Code block at 174-184 with `mcpServers`+`type:http` which is CORRECT and must stay; OpenCode block at 186-197 with `mcp`+`type:http` which is WRONG; the "Other MCP clients" note at 200; the `?workspace=` guidance at 202+ which stays)
    - docs/reference-readme.md:160-178 (the `mcp` block with `type:http` and the `?workspace=` note that stays)
    - .planning/phases/10-.../10-RESEARCH.md §"Common Pitfalls #1" and §"Code Examples" (OpenCode entry: type:remote, enabled:true)
  </read_first>
  <action>
    In docs/SETUP_AGENT.md, under the `### OpenCode` heading, change the example's `"type": "http"` to `"type": "remote"` (and, to match the generated config, add `"enabled": true` to the entry so the doc mirrors what the code writes). Do NOT change the `### Claude Code` block above it — `mcpServers` + `"type": "http"` is correct for Claude Code. If the "Other MCP clients" note at line 200 states OpenCode uses transport type `http`, scope its `http` claim to Claude Code / generic streamable-HTTP clients and note OpenCode's own key is `"type": "remote"`. In docs/reference-readme.md, the example uses the OpenCode `mcp` top-level key with `"type": "http"` — change that to `"type": "remote"`. Preserve every surrounding line, including the `?workspace=` guidance paragraph. These are the exact-string edits; keep JSON indentation identical to the surrounding block.
  </action>
  <verify>
    <automated>grep -q '"type": "remote"' docs/SETUP_AGENT.md && grep -q '"type": "remote"' docs/reference-readme.md && grep -q '"type": "http"' docs/SETUP_AGENT.md</automated>
  </verify>
  <done>The OpenCode example in each file uses `"type": "remote"`; SETUP_AGENT.md's Claude Code example still uses `"type": "http"`; `?workspace=` guidance and all other lines are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Correct OpenCode-style mcp example in README.md</name>
  <files>README.md</files>
  <read_first>
    - README.md:234-252 (the "Configure Your AI Agent" MCP config example using the `mcp` top-level key with `"type": "http"`, followed by the `?workspace=` paragraph that stays)
  </read_first>
  <action>
    In README.md's MCP config example, the block uses the `mcp` top-level key (OpenCode's schema) with `"type": "http"`. Change it to `"type": "remote"` to match OpenCode's actual schema and the config Plan 02 now generates. Preserve the surrounding `?workspace=` guidance paragraph and all other content verbatim. Keep JSON indentation identical.
  </action>
  <verify>
    <automated>grep -c '"type": "remote"' README.md && ! grep -q '"type": "http"' README.md</automated>
  </verify>
  <done>README.md's `mcp` example uses `"type": "remote"`; no stray `"type": "http"` remains in README.md; `?workspace=` guidance intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs → user's manual config | Users copy these examples into their real client config; an incorrect `type` value produces a silently non-connecting server. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-08 | Information Disclosure / Correctness | Stale `type:http` OpenCode doc example misleading users into a broken config | low | mitigate | Correct the example to `type:remote` in all three doc files so the documented value matches both official OpenCode docs and the config the CLI now generates. |
| T-10-SC | Tampering | npm/pip/cargo installs | low | accept | Docs-only plan; no dependency, no install, no code. |
</threat_model>

<verification>
- All OpenCode MCP examples across the three files use `"type": "remote"`.
- Claude Code's example still uses `"type": "http"`.
- No unrelated doc content changed; `?workspace=` guidance intact.
</verification>

<success_criteria>
- docs/SETUP_AGENT.md, README.md, docs/reference-readme.md all show `"type": "remote"` for OpenCode.
- Docs now agree with the config generated by Plan 02.
</success_criteria>

<output>
Create `.planning/phases/10-interactive-mcp-client-auto-configuration-after-workspace-re/10-03-SUMMARY.md` when done.
</output>
