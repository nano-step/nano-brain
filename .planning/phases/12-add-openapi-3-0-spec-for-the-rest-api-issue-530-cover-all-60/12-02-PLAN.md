---
phase: 12-add-openapi-3-0-spec-for-the-rest-api-issue-530-cover-all-60
plan: 02
type: execute
wave: 2
depends_on: [12-01]
autonomous: true
requirements: [D-03, D-04]
tags: [openapi, swag, go, annotations]
files_modified:
  - internal/server/handlers/workspace.go
  - internal/server/handlers/workspace_remove.go
  - internal/server/handlers/workspace_resolve.go
  - internal/server/handlers/reset_workspace.go
  - internal/server/handlers/config.go
  - internal/server/handlers/doctor.go
  - internal/server/handlers/collection.go
  - internal/server/handlers/documents.go
  - internal/server/handlers/get_document.go
  - internal/server/handlers/multi_get.go
  - internal/server/handlers/document.go
  - internal/server/handlers/symbols.go
  - internal/server/handlers/tags.go
  - internal/server/handlers/wakeup.go
  - internal/server/handlers/ticket.go
  - internal/server/handlers/harvest.go
  - internal/server/handlers/reload.go
  - internal/server/handlers/events.go
  - internal/server/handlers/protocol_doc.go
user_setup: []

must_haves:
  truths:
    - "Every route in the core group (top-level status routes + workspace/config/doctor/collection/document/symbol/tag/wakeup/ticket/harvest/reload/events routes under /api/v1) carries swag annotations with @Summary, @Router (full mounted path), method, and request/response schema where a typed struct exists (D-03, AC-2)"
    - "Routes behind workspaceMiddleware carry @Security WorkspaceAuth; routes behind workspaceRegisteredMiddleware+csrfMW carry both WorkspaceRegisteredAuth and CSRFToken (D-04)"
    - "The 5 protocol-tunnel route registrations (/mcp GET/POST/DELETE, /sse GET/POST) have their PRESENCE documented via a placeholder-anchor function, no full request/response schema (D-03, Pattern 2)"
    - "The whole project still builds and the existing test suite stays green — annotations are pure comments, zero behavior change"
  artifacts:
    - "swag doc-comments above every exported handler constructor/method in this plan's 18 handler files"
    - "internal/server/handlers/protocol_doc.go — placeholder-anchor function(s) documenting /mcp and /sse presence per RESEARCH Pattern 2"
  key_links:
    - "@Router paths MUST use the full mounted path from PATTERNS.md's Route-to-group table (e.g. /api/v1/workspaces, not /workspaces) — swag has no visibility into routes.go Group() nesting (Pitfall 3)"
    - "@Security scheme names MUST exactly match the @securityDefinitions names declared in doc.go (WorkspaceAuth / WorkspaceRegisteredAuth / CSRFToken) or swag drops them"
---

<objective>
Annotate the "core" handler group — the top-level status routes and the workspace/config/doctor/collections/documents/symbols/tags/wakeup/ticket/harvest/reload/events routes — plus document the protocol-tunnel routes (/mcp, /sse) via a placeholder anchor.

This is one of two parallel annotation plans (02 = core group, 03 = graph/search group). The two plans touch disjoint handler files so they run in the same wave without conflict. Both depend on Plan 01's confirmed swag template, doc.go @securityDefinitions, and the (now-approved) Assumption A1 outcome.

Purpose: Cover ~half the ~60 routes with path/method/description/schema/security annotations (D-03, D-04, AC-2). No spec regeneration or committing docs/openapi.json here — Plan 04 does the final regen after both annotation plans land, keeping this plan's files disjoint from Plan 03's.
Output: Fully annotated core-group handlers + protocol-tunnel documentation anchor.
</objective>

<execution_context>
@/Users/tamlh/workspaces/self/AI/Tools/nano-brain/.claude/gsd-core/workflows/execute-plan.md
@/Users/tamlh/workspaces/self/AI/Tools/nano-brain/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-add-openapi-3-0-spec-for-the-rest-api-issue-530-cover-all-60/12-CONTEXT.md
@.planning/phases/12-add-openapi-3-0-spec-for-the-rest-api-issue-530-cover-all-60/12-RESEARCH.md
@.planning/phases/12-add-openapi-3-0-spec-for-the-rest-api-issue-530-cover-all-60/12-PATTERNS.md
@.planning/phases/12-add-openapi-3-0-spec-for-the-rest-api-issue-530-cover-all-60/12-01-SUMMARY.md

# Source files (read_first before editing)
@internal/server/routes.go
@internal/server/handlers/query.go
@internal/server/handlers/health.go
</context>

<artifacts_this_phase_produces>
This plan (12-02) introduces:
- **swag doc-comment blocks** (no Go code, no new imports) above the exported handler functions in: `workspace.go` (InitWorkspace, ListWorkspaces), `workspace_remove.go` (RemoveWorkspace), `workspace_resolve.go` (ResolveWorkspace), `reset_workspace.go` (ResetWorkspace), `config.go` (GetConfig, PatchConfig), `doctor.go` (Doctor), `collection.go` (AddCollection, ListCollectionsHandler, RenameCollectionHandler, RemoveCollection), `documents.go` (ListDocuments, DeleteDocument), `get_document.go` (GetDocument), `multi_get.go` (MultiGet), `document.go` (WriteDocument), `symbols.go` (ListSymbols), `tags.go` (ListTags), `wakeup.go` (WakeUpHandler — mounted at both GET /api/v1/wake-up and POST /api/v1/wake-up), `ticket.go` (TicketHandler), `harvest.go` (TriggerHarvest), `reload.go` (ReloadConfig), `events.go` (EventsHandler).
- **New file `internal/server/handlers/protocol_doc.go`** — unexported placeholder-anchor function(s) carrying @Router annotations for /mcp (GET/POST/DELETE) and /sse (GET/POST), documenting presence only (RESEARCH Pattern 2 / Pitfall 4).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Annotate workspace, config, doctor, and status routes (top-level + /api/v1 unauthenticated + workspace-scoped)</name>
  <read_first>
    - internal/server/routes.go (lines 27-51, 84-129 — exact registrations + group membership for these routes)
    - internal/server/handlers/workspace.go, workspace_remove.go, workspace_resolve.go, reset_workspace.go, config.go, doctor.go (handler signatures + their Request/Response struct names)
    - .planning/phases/12-.../12-PATTERNS.md Route-to-group table (lines 175-200) — canonical @Router path + middleware tier per route
    - .planning/phases/12-.../12-01-SUMMARY.md (confirmed annotation template + Assumption A1 outcome)
  </read_first>
  <files>internal/server/handlers/workspace.go, internal/server/handlers/workspace_remove.go, internal/server/handlers/workspace_resolve.go, internal/server/handlers/reset_workspace.go, internal/server/handlers/config.go, internal/server/handlers/doctor.go</files>
  <action>
    Add swag doc-comments above each exported handler constructor, following the confirmed template from 12-01-SUMMARY.md and RESEARCH Pattern 1. Per handler include: `@Summary`, `@Description` (brief), `@Tags` (group by resource: workspaces, config, doctor), `@Accept json`/`@Produce json` as applicable, `@Param request body <RequestType> true "..."` for POST/PUT/PATCH bodies (reference the ACTUAL struct name in this package), `@Param hash path string true "..."` for path params (e.g. DELETE /workspaces/:hash → path param `hash`), `@Success 200 {object} <ResponseType>`, and `@Router <full-path> [method]`.

    Use the PATTERNS.md Route-to-group table for the exact full paths (Pitfall 3 — swag can't see routes.go groups):
      - InitWorkspace → `/api/v1/init [post]` (group `api`, no workspace middleware → no @Security)
      - ListWorkspaces → `/api/v1/workspaces [get]` (group `api` → no @Security)
      - ResolveWorkspace → `/api/v1/workspaces/resolve [post]` (group `api` → no @Security)
      - RemoveWorkspace → `/api/v1/workspaces/{hash} [delete]` (group `api` → no @Security; note swag path-param syntax uses {hash} not :hash)
      - ResetWorkspace → `/api/v1/reset-workspace [post]` (group `api` → no @Security)
      - GetConfig → `/api/v1/config [get]`, PatchConfig → `/api/v1/config [post]` (group `api` → no @Security)
      - Doctor → `/api/v1/doctor [get]` (group `api` → no @Security)

    For error bodies use `@Failure 400 {object} map[string]string` or `{string} string` — the codebase has NO shared handlers.ErrorResponse type (do not reference one). Where a handler has no typed request/response struct, provide at minimum @Summary + @Router + method + @Description (AC-2 floor). Do not change any handler logic — annotations are comments only, add no imports.
  </action>
  <verify>
    <automated>go build ./internal/server/... && gofmt -l internal/server/handlers/workspace.go internal/server/handlers/config.go internal/server/handlers/doctor.go internal/server/handlers/reset_workspace.go internal/server/handlers/workspace_remove.go internal/server/handlers/workspace_resolve.go</automated>
  </verify>
  <acceptance_criteria>
    - All 6 files build; gofmt reports no formatting issues.
    - Every exported handler in these files has @Summary + @Router (full /api/v1 path) + method.
    - swag {hash} path-param syntax used for RemoveWorkspace, not :hash.
    - No reference to a non-existent handlers.ErrorResponse.
  </acceptance_criteria>
  <done>Workspace/config/doctor handlers annotated with correct full paths and no @Security (these are in the api group, no workspace middleware).</done>
</task>

<task type="auto">
  <name>Task 2: Annotate collection, document, symbol, tag, wakeup, ticket, harvest, reload, events routes (with @Security per middleware tier)</name>
  <read_first>
    - internal/server/routes.go (lines 61-129 — data group = api+workspaceMiddleware; write group = data+workspaceRegisteredMiddleware+csrfMW; plus top-level /api/harvest, /api/reload-config)
    - handler signatures + struct names in: collection.go, documents.go, get_document.go, multi_get.go, document.go, symbols.go, tags.go, wakeup.go, ticket.go, harvest.go, reload.go, events.go
    - .planning/phases/12-.../12-PATTERNS.md Route-to-group table
  </read_first>
  <files>internal/server/handlers/collection.go, internal/server/handlers/documents.go, internal/server/handlers/get_document.go, internal/server/handlers/multi_get.go, internal/server/handlers/document.go, internal/server/handlers/symbols.go, internal/server/handlers/tags.go, internal/server/handlers/wakeup.go, internal/server/handlers/ticket.go, internal/server/handlers/harvest.go, internal/server/handlers/reload.go, internal/server/handlers/events.go</files>
  <action>
    Annotate each exported handler with the same annotation shape as Task 1, applying @Security by middleware tier (D-04) per the PATTERNS.md Route-to-group table:
      - `data` group (api + workspaceMiddleware) → add `@Security WorkspaceAuth`. Applies to: AddCollection (POST /api/v1/collections), ListCollectionsHandler (GET /api/v1/collections), RenameCollectionHandler (PUT /api/v1/collections/{name}), RemoveCollection (DELETE /api/v1/collections/{name}), ListTags (GET /api/v1/tags), ListDocuments (GET /api/v1/documents), DeleteDocument (DELETE /api/v1/documents/{id}), GetDocument (POST /api/v1/get), MultiGet (POST /api/v1/multi-get), ListSymbols (GET /api/v1/symbols), EventsHandler (GET /api/v1/events), WakeUpHandler POST (POST /api/v1/wake-up).
      - `write` group (data + workspaceRegisteredMiddleware + csrfMW) → add BOTH `@Security WorkspaceRegisteredAuth` AND `@Security CSRFToken`. Applies to: WriteDocument (POST /api/v1/write).
      - `api` group (no workspace middleware) → no @Security. Applies to: WakeUpHandler GET (GET /api/v1/wake-up), TicketHandler (GET /api/v1/sessions/by-ticket).
      - top-level (no group, no auth by default) → no @Security. Applies to: TriggerHarvest (POST /api/harvest — note: NOT under /api/v1), ReloadConfig (POST /api/reload-config).

    WakeUpHandler is registered on BOTH `api.GET("/wake-up")` and `data.POST("/wake-up")` (routes.go lines 122-124) — the same constructor backs two routes with different middleware tiers. Add TWO @Router lines to the single WakeUpHandler doc block: `@Router /api/v1/wake-up [get]` and `@Router /api/v1/wake-up [post]`, and reflect that the POST path is workspace-scoped in the @Description (swag attaches the security block per-operation; if per-method security differs and swag cannot express it cleanly on a dual-router block, annotate the GET as unsecured and note the POST's workspace requirement in @Description — this is an AC-2 "document presence" floor, acceptable per D-03).

    EventsHandler is an SSE stream (GET /api/v1/events) — document it with @Produce text/event-stream, @Summary, @Router, @Security WorkspaceAuth; no JSON response body schema (it streams). WriteDocument is the only write-group route in this plan — ensure it gets both write-tier security schemes. Reference real struct names for @Param/@Success; use map[string]string for errors; no handlers.ErrorResponse.
  </action>
  <verify>
    <automated>go build ./internal/server/... && go test -race -short ./internal/server/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - All 12 files build and existing server tests stay green (annotations are comment-only).
    - data-group handlers carry @Security WorkspaceAuth; WriteDocument carries WorkspaceRegisteredAuth + CSRFToken.
    - TriggerHarvest/ReloadConfig use their top-level paths (/api/harvest, /api/reload-config), NOT /api/v1/*.
    - WakeUpHandler has both GET and POST @Router lines.
  </acceptance_criteria>
  <done>Data/write/top-level handlers annotated with correct paths and @Security tiers matching real middleware.</done>
</task>

<task type="auto">
  <name>Task 3: Document protocol-tunnel routes (/mcp, /sse) via placeholder-anchor function (D-03, Pattern 2)</name>
  <read_first>
    - internal/server/routes.go lines 131-143 (/sse GET/POST and /mcp GET/POST/DELETE via echo.WrapHandler — no named handler to attach comments to)
    - .planning/phases/12-.../12-RESEARCH.md lines 214-229 (Pattern 2 placeholder-anchor) and Assumption A2
    - .planning/phases/12-.../12-01-SUMMARY.md (confirm whether Plan 01's spike validated the placeholder-anchor approach; if not validated there, validate here before scaling)
  </read_first>
  <files>internal/server/handlers/protocol_doc.go</files>
  <action>
    Create `internal/server/handlers/protocol_doc.go` in package `handlers`. Per RESEARCH Pattern 2 (Pitfall 4): the /mcp and /sse routes are registered inline via `echo.WrapHandler(...)` with no named handler function, so swag needs a doc-anchor. Add unexported placeholder function(s) (e.g. `func mcpProtocolDoc() {}` and `func sseProtocolDoc() {}`) that are never called — purely documentation anchors — each carrying @Summary describing it as a protocol endpoint (not a typical JSON REST API), @Tags protocol, and the @Router lines:
      - MCP anchor: `@Router /mcp [get]`, `@Router /mcp [post]`, `@Router /mcp [delete]`
      - SSE anchor: `@Router /sse [get]`, `@Router /sse [post]`
    Per D-03: document presence only — NO full request/response schemas (these are protocol tunnels, not JSON endpoints). Add a `//nolint:unused` directive or equivalent if the linter flags the never-called functions (they exist solely as swag doc anchors — note this in a code comment).

    Per RESEARCH Assumption A2: if Plan 01's spike did NOT already confirm swag parses a placeholder-anchor function, verify it here by running `make generate-openapi` locally and confirming /mcp and /sse paths appear in the output. If the placeholder-anchor approach fails, fall back to manually patching the 5 paths into the conversion step in internal/openapigen/generate.go (RESEARCH flags this as an acceptable low-risk fallback since D-03 only needs presence). Do NOT leave /mcp and /sse missing from the spec.

    The /ui route (routes.go line 145) is a static HTML redirect, not a JSON REST endpoint — per PATTERNS.md it may be excluded from the spec entirely (planner's call). Exclude it: do not annotate /ui. Note this exclusion in the SUMMARY so Plan 04's route-count reconciliation accounts for it.
  </action>
  <verify>
    <automated>go build ./internal/server/... && go vet ./internal/server/handlers/...</automated>
  </verify>
  <acceptance_criteria>
    - protocol_doc.go builds; never-called anchor functions do not break the build or lint.
    - @Router lines cover /mcp [get/post/delete] and /sse [get/post].
    - /ui is deliberately excluded and the exclusion is documented for Plan 04.
  </acceptance_criteria>
  <done>Protocol-tunnel presence documented per D-03; /ui exclusion recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| annotation text → generated spec | @Security annotations declare the real access requirements; a wrong/missing tier misrepresents the API's protection to clients |
| handler files → build | annotations are comments; a malformed swag tag can still be a valid Go comment but produce a broken spec at generation time (caught in Plan 04's drift/validate tests) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-02-01 | Information Disclosure (misrepresentation) | @Security annotations vs real middleware | medium | mitigate | @Security tiers derived directly from the routes.go group membership in PATTERNS.md's Route-to-group table, not guessed; write-group routes get both WorkspaceRegisteredAuth + CSRFToken per D-04. |
| T-12-02-02 | Information Disclosure | @Description / example values in annotations | medium | mitigate | Descriptions are generic; no real workspace hashes/paths in any example (T-12-01 mitigation carried forward; final review at Plan 04 human-verify). |
| T-12-02-03 | Tampering | protocol-tunnel routes silently missing from spec | low | mitigate | Placeholder-anchor documents /mcp+/sse presence; if swag can't parse the anchor, fall back to a manual path patch in generate.go — never omit (D-03). |
</threat_model>

<verification>
- `go build ./internal/server/...` — all annotated files compile.
- `go test -race -short ./internal/server/... -count=1` — annotations are comment-only, no behavior change, existing tests green.
- Spot-check via `make generate-openapi` locally that this plan's routes appear with correct paths + @Security (Plan 04 formalizes this with the drift + count tests).
</verification>

<success_criteria>
- All 18 core-group handler files annotated with correct full @Router paths and per-tier @Security.
- Protocol-tunnel routes documented; /ui excluded.
- Project builds; existing tests green.
- Disjoint from Plan 03's files (no shared handler files) so both run in Wave 2.
</success_criteria>

<output>
Create `.planning/phases/12-add-openapi-3-0-spec-for-the-rest-api-issue-530-cover-all-60/12-02-SUMMARY.md` when done. Record: which routes annotated, the /ui exclusion, and whether the protocol-tunnel placeholder-anchor worked or needed the fallback (Assumption A2 outcome).
</output>
