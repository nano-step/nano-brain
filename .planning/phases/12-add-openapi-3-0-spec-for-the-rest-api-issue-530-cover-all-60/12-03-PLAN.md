---
phase: 12-add-openapi-3-0-spec-for-the-rest-api-issue-530-cover-all-60
plan: 03
type: execute
wave: 2
depends_on: [12-01]
autonomous: true
requirements: [D-03, D-04]
tags: [openapi, swag, go, annotations]
files_modified:
  - internal/server/handlers/query.go
  - internal/server/handlers/search.go
  - internal/server/handlers/bm25.go
  - internal/server/handlers/graph.go
  - internal/server/handlers/graph_overview.go
  - internal/server/handlers/graph_neighborhood.go
  - internal/server/handlers/graph_pagerank.go
  - internal/server/handlers/impact.go
  - internal/server/handlers/trace.go
  - internal/server/handlers/flow.go
  - internal/server/handlers/flowchart.go
  - internal/server/handlers/links.go
  - internal/server/handlers/embed.go
  - internal/server/handlers/reindex.go
  - internal/server/handlers/reindex_cfg.go
  - internal/server/handlers/summarize.go
  - internal/server/handlers/code_summarize.go
  - internal/server/handlers/code_summarize_status.go
  - internal/server/handlers/code_summarize_failures.go
  - internal/server/handlers/code_summarize_retry.go
  - internal/server/handlers/stats.go
user_setup: []

must_haves:
  truths:
    - "Every route in the graph/search/embed/summarize group under /api/v1 carries swag annotations with @Summary, @Router (full mounted path), method, and request/response schema where a typed struct exists (D-03, AC-2)"
    - "Read routes in the data group carry @Security WorkspaceAuth; write routes in the write group carry both WorkspaceRegisteredAuth and CSRFToken (D-04)"
    - "The whole project still builds and the existing test suite stays green — annotations are pure comments, zero behavior change"
  artifacts:
    - "swag doc-comments above every exported handler constructor/method in this plan's 21 handler files (search/graph/flow/links/embed/reindex/summarize/code-summarize/stats)"
  key_links:
    - "@Router paths MUST use the full mounted path from PATTERNS.md's Route-to-group table (/api/v1/...) — Pitfall 3"
    - "@Security tiers MUST match each route's actual routes.go group (data vs write) — write-tier code/summarize routes need both WorkspaceRegisteredAuth and CSRFToken"
    - "stats.go uses the NewStatsHandler + (h *StatsHandler) Handle method-receiver idiom (like health.go), NOT the constructor-returns-closure idiom — annotate the Handle method"
---

<objective>
Annotate the "graph/search" handler group — hybrid query, vector/BM25 search, all graph/flow/impact/trace/links routes, embed, reindex, summarize, code-summarize (5 routes), and stats.

This is the second of two parallel annotation plans (02 = core group, 03 = this graph/search group). The two plans touch disjoint handler files so they run in the same wave (Wave 2) without conflict. Both depend on Plan 01's confirmed swag template, doc.go @securityDefinitions, and approved Assumption A1.

Purpose: Cover the remaining ~half of the ~60 routes with path/method/description/schema/security annotations (D-03, D-04, AC-2). No spec regeneration or committing docs/openapi.json here — Plan 04 does the final regen after both annotation plans land.
Output: Fully annotated graph/search/embed/summarize/stats handlers.
</objective>

<execution_context>
@/Users/tamlh/workspaces/self/AI/Tools/nano-brain/.claude/gsd-core/workflows/execute-plan.md
@/Users/tamlh/workspaces/self/AI/Tools/nano-brain/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-add-openapi-3-0-spec-for-the-rest-api-issue-530-cover-all-60/12-CONTEXT.md
@.planning/phases/12-add-openapi-3-0-spec-for-the-rest-api-issue-530-cover-all-60/12-RESEARCH.md
@.planning/phases/12-add-openapi-3-0-spec-for-the-rest-api-issue-530-cover-all-60/12-PATTERNS.md
@.planning/phases/12-add-openapi-3-0-spec-for-the-rest-api-issue-530-cover-all-60/12-01-SUMMARY.md

# Source files (read_first before editing)
@internal/server/routes.go
@internal/server/handlers/query.go
@internal/server/handlers/health.go
</context>

<artifacts_this_phase_produces>
This plan (12-03) introduces swag doc-comment blocks (no Go code, no new imports) above the exported handler functions in:
- **search:** query.go (Query → POST /api/v1/query), search.go (VectorSearch → POST /api/v1/vsearch), bm25.go (BM25Search → POST /api/v1/search)
- **graph:** graph.go (GraphQuery → POST /api/v1/graph/query), graph_overview.go (GraphOverview → POST /api/v1/graph/overview), impact.go (GraphImpact → POST /api/v1/graph/impact), trace.go (GraphTrace → POST /api/v1/graph/trace), graph_neighborhood.go (GraphNeighborhood → POST /api/v1/graph/neighborhood [write]), graph_pagerank.go (GraphPageRankCompute → POST /api/v1/graph/pagerank/compute [write])
- **flow:** flow.go (GraphFlow → POST /api/v1/graph/flow; FlowMaterialize → POST /api/v1/flow/materialize [write]; ListFlowEndpoints → GET /api/v1/graph/flow/endpoints), flowchart.go (GraphFlowchart → POST /api/v1/graph/flowchart)
- **links:** links.go (Backlinks → GET /api/v1/links/{doc_id}/backlinks; ResolveLink → GET /api/v1/links/resolve)
- **embed/reindex:** embed.go (TriggerEmbed → POST /api/v1/embed [write]), reindex.go (TriggerReindex → POST /api/v1/reindex [write]; TriggerUpdate → POST /api/v1/update [write]), reindex_cfg.go (ReindexCFG → POST /api/v1/reindex-cfg [write])
- **summarize:** summarize.go (TriggerSummarize → POST /api/v1/summarize [write])
- **code-summarize:** code_summarize.go (TriggerCodeSummarize → POST /api/v1/code/summarize [write]), code_summarize_status.go (GetCodeSummarizeStatus → GET /api/v1/code/summarize/status [write]), code_summarize_failures.go (GetCodeSummarizeFailures → GET /api/v1/code/summarize/failures [write]), code_summarize_retry.go (RetryCodeSummarize → POST /api/v1/code/summarize/retry [write]; RetryAllCodeSummarize → POST /api/v1/code/summarize/retry-all [write])
- **stats:** stats.go ((h *StatsHandler) Handle → GET /api/v1/stats [data])
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Annotate search + graph read routes (data group, @Security WorkspaceAuth)</name>
  <read_first>
    - internal/server/routes.go lines 95-114 (graph/search registrations; data group = api + workspaceMiddleware)
    - handler signatures + Request/Response struct names in: query.go, search.go, bm25.go, graph.go, graph_overview.go, impact.go, trace.go, flowchart.go
    - .planning/phases/12-.../12-PATTERNS.md Route-to-group table + query.go annotation example (lines 62-77)
    - .planning/phases/12-.../12-01-SUMMARY.md (confirmed template)
  </read_first>
  <files>internal/server/handlers/query.go, internal/server/handlers/search.go, internal/server/handlers/bm25.go, internal/server/handlers/graph.go, internal/server/handlers/graph_overview.go, internal/server/handlers/impact.go, internal/server/handlers/trace.go, internal/server/handlers/flowchart.go</files>
  <action>
    Annotate each exported handler constructor with @Summary, @Description, @Tags (search / graph), @Accept json, @Produce json, @Param request body <RequestType> true "...", @Success 200 {object} <ResponseType>, @Security WorkspaceAuth (all of these are in the `data` group — api + workspaceMiddleware, per routes.go), and @Router with the full path from PATTERNS.md:
      - Query → `/api/v1/query [post]`, VectorSearch → `/api/v1/vsearch [post]`, BM25Search → `/api/v1/search [post]`
      - GraphQuery → `/api/v1/graph/query [post]`, GraphOverview → `/api/v1/graph/overview [post]`, GraphImpact → `/api/v1/graph/impact [post]`, GraphTrace → `/api/v1/graph/trace [post]`, GraphFlowchart → `/api/v1/graph/flowchart [post]`
    Reference the REAL request/response struct names in each file (e.g. query.go's QueryRequest/SearchResponse). Use `@Failure 400 {object} map[string]string` for errors — no handlers.ErrorResponse (does not exist). The query.go annotation in PATTERNS.md is a template but its `@Failure 400 {object} handlers.ErrorResponse` line is WRONG (that type doesn't exist) — replace with map[string]string. Comments only; add no imports; change no logic.
  </action>
  <verify>
    <automated>go build ./internal/server/... && gofmt -l internal/server/handlers/query.go internal/server/handlers/search.go internal/server/handlers/bm25.go internal/server/handlers/graph.go internal/server/handlers/graph_overview.go internal/server/handlers/impact.go internal/server/handlers/trace.go internal/server/handlers/flowchart.go</automated>
  </verify>
  <acceptance_criteria>
    - All 8 files build; gofmt clean.
    - Each handler carries @Security WorkspaceAuth + full /api/v1 @Router path + real struct schemas.
    - No handlers.ErrorResponse reference.
  </acceptance_criteria>
  <done>Search + graph read routes annotated as data-group (WorkspaceAuth) with correct paths.</done>
</task>

<task type="auto">
  <name>Task 2: Annotate flow, links, neighborhood, pagerank routes (mixed data + write tiers)</name>
  <read_first>
    - internal/server/routes.go lines 99-120 (flow.go GraphFlow=data, FlowMaterialize=write, ListFlowEndpoints=data; links=data; graph_neighborhood + graph_pagerank = write)
    - handler signatures + struct names in: flow.go, links.go, graph_neighborhood.go, graph_pagerank.go
    - .planning/phases/12-.../12-PATTERNS.md Route-to-group table
  </read_first>
  <files>internal/server/handlers/flow.go, internal/server/handlers/links.go, internal/server/handlers/graph_neighborhood.go, internal/server/handlers/graph_pagerank.go</files>
  <action>
    Annotate per the exact routes.go group membership (Pitfall 3):
      - GraphFlow → `/api/v1/graph/flow [post]`, @Security WorkspaceAuth (data group)
      - ListFlowEndpoints → `/api/v1/graph/flow/endpoints [get]`, @Security WorkspaceAuth (data group)
      - FlowMaterialize → `/api/v1/flow/materialize [post]`, @Security WorkspaceRegisteredAuth + @Security CSRFToken (WRITE group — routes.go line 102 registers it on `write`)
      - Backlinks → `/api/v1/links/{doc_id}/backlinks [get]`, @Security WorkspaceAuth (data group); @Param doc_id path string true "..." (swag {doc_id} syntax, not :doc_id)
      - ResolveLink → `/api/v1/links/resolve [get]`, @Security WorkspaceAuth (data group)
      - GraphNeighborhood → `/api/v1/graph/neighborhood [post]`, @Security WorkspaceRegisteredAuth + CSRFToken (WRITE group — routes.go line 116)
      - GraphPageRankCompute → `/api/v1/graph/pagerank/compute [post]`, @Security WorkspaceRegisteredAuth + CSRFToken (WRITE group — routes.go line 115)
    Reference real request/response struct names; @Failure 400 {object} map[string]string for errors; comments only, no imports, no logic change. Pay attention: neighborhood and pagerank are WRITE-tier despite being graph operations — do not assume all graph routes are data-tier.
  </action>
  <verify>
    <automated>go build ./internal/server/... && go test -race -short ./internal/server/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - All 4 files build; existing server tests green.
    - FlowMaterialize, GraphNeighborhood, GraphPageRankCompute carry BOTH WorkspaceRegisteredAuth and CSRFToken (write tier).
    - GraphFlow, ListFlowEndpoints, Backlinks, ResolveLink carry WorkspaceAuth (data tier).
    - Path params use swag {name} syntax.
  </acceptance_criteria>
  <done>Flow/links/neighborhood/pagerank annotated with correct per-route tiers (data vs write).</done>
</task>

<task type="auto">
  <name>Task 3: Annotate embed, reindex, summarize, code-summarize, and stats routes</name>
  <read_first>
    - internal/server/routes.go lines 68-114 (embed/reindex/summarize/code-summarize are all WRITE group; stats is data group)
    - handler signatures + struct names in: embed.go, reindex.go, reindex_cfg.go, summarize.go, code_summarize.go, code_summarize_status.go, code_summarize_failures.go, code_summarize_retry.go, stats.go
    - internal/server/handlers/health.go (StatsHandler uses the same method-receiver idiom as Health — annotate the Handle method, per PATTERNS.md)
  </read_first>
  <files>internal/server/handlers/embed.go, internal/server/handlers/reindex.go, internal/server/handlers/reindex_cfg.go, internal/server/handlers/summarize.go, internal/server/handlers/code_summarize.go, internal/server/handlers/code_summarize_status.go, internal/server/handlers/code_summarize_failures.go, internal/server/handlers/code_summarize_retry.go, internal/server/handlers/stats.go</files>
  <action>
    Annotate the WRITE-group handlers (routes.go registers all of these on `write` = data + workspaceRegisteredMiddleware + csrfMW) with @Security WorkspaceRegisteredAuth + @Security CSRFToken:
      - TriggerEmbed → `/api/v1/embed [post]`
      - TriggerReindex → `/api/v1/reindex [post]`, TriggerUpdate → `/api/v1/update [post]` (both in reindex.go)
      - ReindexCFG → `/api/v1/reindex-cfg [post]`
      - TriggerSummarize → `/api/v1/summarize [post]`
      - TriggerCodeSummarize → `/api/v1/code/summarize [post]`
      - GetCodeSummarizeStatus → `/api/v1/code/summarize/status [get]`
      - GetCodeSummarizeFailures → `/api/v1/code/summarize/failures [get]`
      - RetryCodeSummarize → `/api/v1/code/summarize/retry [post]`, RetryAllCodeSummarize → `/api/v1/code/summarize/retry-all [post]` (both in code_summarize_retry.go)

    Annotate the DATA-group stats handler with @Security WorkspaceAuth:
      - stats.go's `(h *StatsHandler) Handle` method → `/api/v1/stats [get]` (routes.go line 114: `data.GET("/stats", statsH.Handle)`). This uses the method-receiver idiom (like health.go's Health/Status/Version), so the annotation goes directly above `func (h *StatsHandler) Handle(c echo.Context) error`, NOT above NewStatsHandler.

    Standard annotation shape for each: @Summary, @Description, @Tags (embed / reindex / summarize / stats), @Accept json / @Produce json as applicable, @Param request body <RequestType> for POST bodies with typed requests, @Success 200 {object} <ResponseType>, @Failure 400 {object} map[string]string, the @Security tier above, and @Router. Reference real struct names only; where a handler lacks a typed request/response struct, provide the AC-2 floor (@Summary + @Router + method + @Description). Comments only; no imports; no logic change.
  </action>
  <verify>
    <automated>go build ./internal/server/... && go test -race -short ./internal/server/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - All 9 files build; existing server tests green.
    - embed/reindex/reindex-cfg/summarize/code-summarize (all 9 write routes) carry WorkspaceRegisteredAuth + CSRFToken.
    - stats carries WorkspaceAuth and is annotated on the Handle method receiver.
    - No handlers.ErrorResponse reference.
  </acceptance_criteria>
  <done>Embed/reindex/summarize/code-summarize (write tier) and stats (data tier) annotated with correct paths and idioms.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| annotation text → generated spec | @Security tier declares real access requirements; misrepresenting a write route as unauthenticated would understate the API's protection |
| handler files → build | annotations are comments; a malformed swag tag is still valid Go but yields a broken spec (caught in Plan 04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-03-01 | Information Disclosure (misrepresentation) | @Security tiers on write-group graph/embed/summarize routes | medium | mitigate | Write-tier routes (embed, reindex, summarize, code-summarize, neighborhood, pagerank, flow/materialize) get both WorkspaceRegisteredAuth + CSRFToken derived directly from routes.go `write` group membership, not guessed (D-04). |
| T-12-03-02 | Information Disclosure | @Description / example values | medium | mitigate | Generic descriptions; no real workspace hashes/paths in examples (T-12-01 carried forward; final review at Plan 04). |
</threat_model>

<verification>
- `go build ./internal/server/...` — all 21 annotated files compile.
- `go test -race -short ./internal/server/... -count=1` — comment-only changes, existing tests green.
- Spot-check via `make generate-openapi` locally that this plan's routes appear with correct paths + @Security (Plan 04 formalizes with drift + count tests).
</verification>

<success_criteria>
- All 21 graph/search-group handler files annotated with correct full @Router paths and per-tier @Security.
- Write-tier routes carry both write-group security schemes; data-tier routes carry WorkspaceAuth.
- Project builds; existing tests green.
- Disjoint from Plan 02's files (no shared handler files) so both run in Wave 2.
</success_criteria>

<output>
Create `.planning/phases/12-add-openapi-3-0-spec-for-the-rest-api-issue-530-cover-all-60/12-03-SUMMARY.md` when done. Record which routes annotated and any handler that lacked a typed struct (annotated at the AC-2 floor).
</output>
