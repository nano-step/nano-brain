---
phase: 12-add-openapi-3-0-spec-for-the-rest-api-issue-530-cover-all-60
plan: 04
type: execute
wave: 3
depends_on: [12-01, 12-02, 12-03]
autonomous: false
requirements: [D-02, D-05, D-06]
tags: [openapi, echo, serve, docs, embed, drift]
files_modified:
  - docs/openapi.json
  - internal/server/handlers/openapi.go
  - internal/server/handlers/openapi_test.go
  - internal/server/routes.go
  - internal/config/defaults.go
  - internal/openapigen/openapi_gen_test.go
  - README.md
  - docs/SETUP_AGENT.md
  - CLAUDE.md
user_setup: []

must_haves:
  truths:
    - "GET /api/openapi.json serves the committed OpenAPI 3.0 spec (D-02: JSON only, no UI)"
    - "The served spec covers ALL ~60 routes (every route in routes.go except /ui) with path+method+description, and request/response schemas where a typed struct exists (D-03, AC-2)"
    - "/api/openapi.json is reachable without auth, consistent with /health (added to default BypassPaths)"
    - "A route-count / path reconciliation test fails if a route is added to routes.go without a matching annotation (D-05, AC-3, Pitfall 3 — checks path strings, not just count)"
    - "README.md and/or docs/SETUP_AGENT.md tell users how to fetch/browse the spec, and CLAUDE.md Quick Reference lists make generate-openapi (D-06, AC-4)"
    - "The final committed docs/openapi.json is regenerated from ALL annotations (Plans 02+03) and still validates against OpenAPI 3.0"
  artifacts:
    - "internal/server/handlers/openapi.go — OpenAPISpec() handler serving embedded openapi.json via c.Blob"
    - "internal/server/handlers/openapi_test.go — TestOpenAPISpecHandler (200, application/json, root openapi 3.0.x)"
    - "docs/openapi.json — final complete OpenAPI 3.0 artifact, regenerated"
    - "route reconciliation assertion in internal/openapigen/openapi_gen_test.go"
    - "doc pointer in README.md / docs/SETUP_AGENT.md + CLAUDE.md Quick Reference line"
  key_links:
    - "//go:embed can only reference paths within the handler's own package dir — the served openapi.json must be colocated in internal/server/handlers/ (embed cannot reach ../../docs/) OR a small wrapper package exports the bytes; make generate-openapi must write to the embedded location"
    - "The new route registration in routes.go must be added to default BypassPaths in internal/config/defaults.go so it stays public when auth is enabled"
    - "The reconciliation test compares routes.go's registered paths against the generated spec's paths — the source of truth link that makes drift detectable (D-05)"
---

<objective>
Finalize the phase: regenerate the complete spec from all annotations, serve it at `GET /api/openapi.json`, make it publicly reachable, prove single-source-of-truth with the route table (D-05/AC-3), test the handler, and document how to fetch/browse it (D-06/AC-4).

This is the Wave 3 convergence plan. It depends on Plans 02 and 03 having annotated all handler files, so the regenerated `docs/openapi.json` covers all ~60 routes. It is the only plan that touches `docs/openapi.json` after Plan 01 and the only one that touches `routes.go`, so it does not conflict with the parallel Wave 2 annotation plans.

Purpose: Close issue #530's four acceptance criteria — servable+valid (AC-1, finalized here), all routes present (AC-2), single-source-of-truth/drift-caught (AC-3), docs mention how to fetch (AC-4).
Output: Served, complete, validated, drift-checked, documented OpenAPI 3.0 spec.
</objective>

<execution_context>
@/Users/tamlh/workspaces/self/AI/Tools/nano-brain/.claude/gsd-core/workflows/execute-plan.md
@/Users/tamlh/workspaces/self/AI/Tools/nano-brain/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-add-openapi-3-0-spec-for-the-rest-api-issue-530-cover-all-60/12-CONTEXT.md
@.planning/phases/12-add-openapi-3-0-spec-for-the-rest-api-issue-530-cover-all-60/12-RESEARCH.md
@.planning/phases/12-add-openapi-3-0-spec-for-the-rest-api-issue-530-cover-all-60/12-PATTERNS.md
@.planning/phases/12-add-openapi-3-0-spec-for-the-rest-api-issue-530-cover-all-60/12-VALIDATION.md
@.planning/phases/12-add-openapi-3-0-spec-for-the-rest-api-issue-530-cover-all-60/12-01-SUMMARY.md
@.planning/phases/12-add-openapi-3-0-spec-for-the-rest-api-issue-530-cover-all-60/12-02-SUMMARY.md
@.planning/phases/12-add-openapi-3-0-spec-for-the-rest-api-issue-530-cover-all-60/12-03-SUMMARY.md

# Source files (read_first before editing)
@internal/server/routes.go
@internal/server/handlers/health.go
@migrations/migrations.go
@internal/config/defaults.go
@internal/server/middleware/auth.go
</context>

<artifacts_this_phase_produces>
This plan (12-04) introduces:
- **New file `internal/server/handlers/openapi.go`** — package `handlers`; `//go:embed openapi.json` (colocated copy) into an `embed.FS`; `func OpenAPISpec() echo.HandlerFunc` reading the embedded bytes once and returning `c.Blob(http.StatusOK, "application/json", specJSON)` per RESEARCH Code Examples + PATTERNS embed precedent. Carries its own swag annotation (@Summary, @Tags meta, @Produce json, @Router /api/openapi.json [get]).
- **New file `internal/server/handlers/openapi_test.go`** — `TestOpenAPISpecHandler` (httptest harness like health_test.go): asserts 200, Content-Type application/json, body is valid JSON with root "openapi":"3.0.x".
- **New route** `s.echo.GET("/api/openapi.json", handlers.OpenAPISpec())` in routes.go, near the /health block.
- **Modified `internal/config/defaults.go`** — add `/api/openapi.json` to the default `BypassPaths` (alongside `/health`), so the discovery endpoint stays public when auth is enabled.
- **Modified `docs/openapi.json`** — final, complete, regenerated OpenAPI 3.0 artifact covering all annotated routes.
- **New reconciliation assertion** in `internal/openapigen/openapi_gen_test.go` — asserts every path registered in routes.go (except /ui and the openapi.json meta route itself) appears in the generated spec's paths (D-05/AC-3, path-string level per Pitfall 3).
- **Modified docs:** README.md + docs/SETUP_AGENT.md prose pointer to `GET /api/openapi.json`; CLAUDE.md Quick Reference gains a `make generate-openapi` line (D-06/AC-4).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate complete spec, create the serving handler + route + auth bypass</name>
  <read_first>
    - internal/server/routes.go lines 27-29 (top-level /health block — where to register the new route) and 145 (/ui, excluded)
    - internal/server/handlers/health.go Version method (simplest handler template) + migrations/migrations.go (//go:embed precedent)
    - internal/config/defaults.go line 22 (BypassPaths: []string{"/health"})
    - internal/server/middleware/auth.go (BypassPaths is matched by exact path equality when auth is Enabled)
    - .planning/phases/12-.../12-RESEARCH.md Code Examples (lines 295-320) + PATTERNS.md openapi.go section (lines 109-148 — the embed-path colocation constraint)
  </read_first>
  <files>docs/openapi.json, internal/server/handlers/openapi.go, internal/server/routes.go, internal/config/defaults.go</files>
  <action>
    Run `make generate-openapi` to regenerate `docs/openapi.json` from ALL annotations now present (Plans 01+02+03). This is the complete spec.

    Resolve the //go:embed path constraint (PATTERNS.md flags it): Go embed directives cannot reference paths outside the package directory, so `internal/server/handlers/openapi.go` CANNOT `//go:embed ../../../docs/openapi.json`. Choose the colocated-copy approach: `make generate-openapi` writes the canonical `docs/openapi.json` AND copies it to `internal/server/handlers/openapi.json` (the embed source). Update the Makefile recipe (from Plan 01) to write both locations, so the embedded bytes always match the committed canonical file — document this in the Makefile comment. (Alternative allowed if cleaner: a small `docs` wrapper package exporting the embedded bytes; but the colocated copy is the simplest and matches migrations.go's in-package embed precedent.)

    Create `internal/server/handlers/openapi.go` (package handlers) per RESEARCH Code Examples + PATTERNS.md lines 122-146: `//go:embed openapi.json` into `var openapiSpecFS embed.FS`; `func OpenAPISpec() echo.HandlerFunc { specJSON, _ := openapiSpecFS.ReadFile("openapi.json"); return func(c echo.Context) error { return c.Blob(http.StatusOK, "application/json", specJSON) } }`. Add its own swag annotation block (@Summary Serve the generated OpenAPI 3.0 specification, @Tags meta, @Produce json, @Router /api/openapi.json [get]) — no @Security (public discovery endpoint).

    Register the route in routes.go near the /health block (line 27-29): `s.echo.GET("/api/openapi.json", handlers.OpenAPISpec())`.

    Add `/api/openapi.json` to the default `BypassPaths` in internal/config/defaults.go (currently `[]string{"/health"}` → `[]string{"/health", "/api/openapi.json"}`), so the spec stays publicly reachable when auth is enabled — consistent with /health per RESEARCH Security Domain V2 note. (Auth is disabled by default; this makes the intent explicit and correct for auth-on deployments.)

    Re-run `make generate-openapi` after adding the openapi.go annotation so the spec includes the /api/openapi.json route itself, then confirm no drift.
  </action>
  <verify>
    <automated>go build ./... && make generate-openapi && git diff --exit-code docs/openapi.json internal/server/handlers/openapi.json</automated>
  </verify>
  <acceptance_criteria>
    - docs/openapi.json regenerated covering all routes; colocated copy at internal/server/handlers/openapi.json matches it byte-for-byte.
    - OpenAPISpec() handler builds and embeds the spec.
    - Route registered; /api/openapi.json added to default BypassPaths.
    - `make generate-openapi` then `git diff --exit-code` shows no drift.
  </acceptance_criteria>
  <done>Complete spec regenerated, served via embedded handler + route, publicly reachable via BypassPaths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Handler test + route reconciliation drift test (single source of truth, D-05/AC-3)</name>
  <read_first>
    - internal/server/handlers/health_test.go (httptest harness: echo.New, httptest.NewRequest/NewRecorder, e.NewContext, decodeJSON)
    - internal/server/routes.go (full route table — the set of registered paths the reconciliation test compares against)
    - internal/openapigen/openapi_gen_test.go (existing TestOpenAPISpec_NoDrift from Plan 01 — extend, do not duplicate)
    - .planning/phases/12-.../12-VALIDATION.md (12-02-02 drift, 12-02-03 handler test rows)
  </read_first>
  <behavior>
    - TestOpenAPISpecHandler: GET /api/openapi.json via httptest → rec.Code == 200, Content-Type == "application/json", body unmarshals as JSON with root "openapi" starting "3.0" and no "swagger" key.
    - Route reconciliation: for every path+method registered in routes.go (excluding /ui and treating protocol tunnels /mcp,/sse as presence-only), the generated spec contains a matching paths entry — a route added without an annotation makes this test FAIL (D-05/AC-3). Compares path strings (Pitfall 3), not just a count.
  </behavior>
  <files>internal/server/handlers/openapi_test.go, internal/openapigen/openapi_gen_test.go</files>
  <action>
    Create `internal/server/handlers/openapi_test.go` (package handlers_test) mirroring health_test.go's harness (PATTERNS.md lines 233-246): construct `handlers.OpenAPISpec()`, `httptest.NewRequest(http.MethodGet, "/api/openapi.json", nil)`, `httptest.NewRecorder()`, `e.NewContext(req, rec)`, call the handler, assert `rec.Code == http.StatusOK`, `rec.Header().Get("Content-Type")` starts with "application/json", and the body parses as JSON with `"openapi"` starting `"3.0"` and no `"swagger"` root key (guards Pitfall 1 at the handler level).

    Extend `internal/openapigen/openapi_gen_test.go` with the reconciliation assertion (D-05/AC-3). Enumerate the expected route set. Since routes.go is Go code (not trivially machine-readable at test time without importing the server package and building a router), the robust approach: build the real Echo router via the server package's route registration and call `echo.Routes()` to get the registered `[]Route{Method, Path}`, then assert each (excluding /ui, and mapping Echo's `:param`/`{param}` syntax to swag's `{param}` for comparison) appears in the generated spec's `paths`. If wiring a full server in the test is too heavy (needs many deps), fall back to a maintained explicit slice of expected paths in the test PLUS a count assertion (spec path count == expected count) — but PREFER the echo.Routes()-derived approach so a new route is caught automatically per D-05's intent. Document which approach was used and why in a test comment.

    Protocol tunnels (/mcp, /sse) and /ui: exclude /ui from the expectation; include /mcp and /sse as presence-only (path exists in spec, no schema assertion). Keep the test in the openapigen package so it runs in the standard `go test -race -short ./...` sweep (no new CI job, per D-05 recommendation).

    Commit test + any spec change together (green commits only).
  </action>
  <verify>
    <automated>go test -race -short ./internal/server/... ./internal/openapigen/... -run 'TestOpenAPISpecHandler|TestOpenAPISpec_NoDrift|TestOpenAPISpec_Routes' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - TestOpenAPISpecHandler passes (200, application/json, openapi 3.0.x root).
    - Reconciliation test passes AND would fail if a routes.go route lacked an annotation (verify by temporarily adding a dummy route locally, confirming red, then reverting).
    - Full drift test still green after the final regeneration.
  </acceptance_criteria>
  <done>Handler served correctly; single-source-of-truth with routes.go enforced by an automated test that catches missing annotations.</done>
</task>

<task type="auto">
  <name>Task 3: Document how to fetch/browse the spec (D-06/AC-4)</name>
  <read_first>
    - README.md around line 497 ("Development Setup" section — where to add the prose pointer, per PATTERNS.md)
    - docs/SETUP_AGENT.md (established numbered agent-setup structure)
    - CLAUDE.md Quick Reference block (the sqlc generate one-liner style to mirror)
    - .planning/phases/12-.../12-PATTERNS.md lines 287-297 (exact doc-line style)
  </read_first>
  <files>README.md, docs/SETUP_AGENT.md, CLAUDE.md</files>
  <action>
    Add a short prose note to README.md's "Development Setup" section (near line 497): that `GET /api/openapi.json` serves the current OpenAPI 3.0 spec for the REST API, importable into Postman / Swagger UI / codegen tools, and that it is regenerated via `make generate-openapi` after adding or changing routes. Mirror the one-liner style already in the repo.

    Add a matching brief pointer to docs/SETUP_AGENT.md so agents discovering the API surface know the spec endpoint exists (D-06 explicitly names SETUP_AGENT.md as an acceptable target).

    Add a Quick Reference line to CLAUDE.md's existing Quick Reference block (mirror the `sqlc generate` line exactly, per PATTERNS.md lines 289-297): `make generate-openapi                                      # OpenAPI spec regeneration`.

    Keep it terse — this is a doc pointer, not a tutorial. Do not add AI footers to any commit (project rule). No real workspace hashes/paths in any example (Security Domain).
  </action>
  <verify>
    <automated>grep -qi 'openapi' README.md docs/SETUP_AGENT.md && grep -q 'generate-openapi' CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md and docs/SETUP_AGENT.md both mention GET /api/openapi.json and how to fetch/regenerate it.
    - CLAUDE.md Quick Reference lists make generate-openapi.
    - No AI footers introduced.
  </acceptance_criteria>
  <done>D-06/AC-4 satisfied — docs tell users how to fetch and regenerate the spec.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Checkpoint — verify served spec is complete, valid, drift-free, and leak-free before ship</name>
  <what-built>
    Complete OpenAPI 3.0 spec regenerated from all ~60 route annotations, served at GET /api/openapi.json (public, in BypassPaths), with a route-reconciliation drift test enforcing single-source-of-truth with routes.go, a handler test, and docs pointers (README/SETUP_AGENT/CLAUDE.md).
  </what-built>
  <how-to-verify>
    1. `make generate-openapi && git diff --exit-code docs/openapi.json` — no drift.
    2. `go test -race -short ./... -count=1` — full suite green (drift, validation, handler, reconciliation tests included).
    3. Build and run locally against the TEST server only (nanobrain_test / :3199 — NEVER dev :3100 per project test-isolation rule): `curl -s localhost:3199/api/openapi.json | head` — confirm it returns JSON with `"openapi": "3.0..."`.
    4. **Information-disclosure review (Security Domain / VALIDATION.md manual-only item):** open docs/openapi.json and scan all example/description values — confirm NO real workspace hashes or filesystem paths appear (only clearly-fake placeholders). This is the one mechanically-uncheckable security gate for this phase.
    5. Spot-check 3-4 routes across tiers: a public route (/health), a data route (/api/v1/query with WorkspaceAuth), and a write route (/api/v1/write with WorkspaceRegisteredAuth + CSRFToken) — confirm their security blocks match reality.
  </how-to-verify>
  <resume-signal>Type "approved" if the spec is complete, valid, drift-free, leak-free, and correctly served; or describe issues found.</resume-signal>
  <files>(none — human verification of docs/openapi.json and the served endpoint produced by Tasks 1-3)</files>
  <action>Human-only verification checkpoint — no code to write. Follow the steps in <how-to-verify>, paying special attention to step 4 (the information-disclosure review of the final spec — the one mechanically-uncheckable security gate). Use the TEST server (:3199) only for the smoke test, never the dev server (:3100), per project test-isolation rules. Do NOT ship until the user approves.</action>
  <verify><human-check>User confirms no drift, full suite green, the served endpoint returns OpenAPI 3.0 JSON on :3199, no real workspace hashes/paths appear in the spec, and spot-checked security tiers match reality, per <how-to-verify>.</human-check></verify>
  <done>User types "approved" after confirming the spec is complete, valid, drift-free, leak-free, publicly served, and security tiers are correct — OR describes issues to fix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → GET /api/openapi.json | unauthenticated clients read the full endpoint enumeration (accepted per issue #530 intent) |
| committed spec → served bytes | embedded openapi.json is what clients receive; any leaked real workspace hash/path ships to every caller |
| routes.go → generated spec | the reconciliation test is the integrity link that keeps the served doc honest as routes change |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-04-01 | Information Disclosure | example/description values in docs/openapi.json | high | mitigate | Human-verify checkpoint step 4 explicitly reviews the final spec for real workspace hashes/paths before ship (VALIDATION.md manual-only item); annotations authored with fake placeholders only. |
| T-12-04-02 | Information Disclosure | full endpoint enumeration via public /api/openapi.json | low | accept | Accepted per issue #530's explicit discoverability intent and D-02 (public JSON, no auth-gated UI); no additional mitigation beyond what applies to any public API doc. |
| T-12-04-03 | Tampering/Integrity | served spec drifts from actual routes | medium | mitigate | Reconciliation + drift tests run in the standard `go test -race -short ./...` sweep and CI; a route added without annotation fails the build gate (D-05/AC-3). |
| T-12-04-04 | Elevation/Access-Control regression | /api/openapi.json exposed but should stay consistent with other metadata routes | low | mitigate | Added to default BypassPaths alongside /health (RESEARCH Security Domain V2); auth-off by default so no regression to existing routes; the new route is read-only and takes no params. |
</threat_model>

<verification>
- `make generate-openapi && git diff --exit-code docs/openapi.json internal/server/handlers/openapi.json` — no drift, colocated copy matches.
- `go test -race -short ./... -count=1` — full suite green including drift, validation, handler, reconciliation tests.
- `grep -qi 'openapi' README.md docs/SETUP_AGENT.md && grep -q 'generate-openapi' CLAUDE.md` — docs pointers present.
- Human-verify checkpoint: served-endpoint smoke test on :3199 + information-disclosure review of the final spec.
</verification>

<success_criteria>
- GET /api/openapi.json serves the complete, valid OpenAPI 3.0 spec covering all ~60 routes (all four issue #530 ACs closed).
- Single-source-of-truth with routes.go enforced by an automated reconciliation test (D-05/AC-3).
- Endpoint public via BypassPaths, consistent with /health.
- Docs tell users how to fetch/regenerate (D-06/AC-4).
- No real workspace hashes/paths leaked in the spec (reviewed at checkpoint).
</success_criteria>

<output>
Create `.planning/phases/12-add-openapi-3-0-spec-for-the-rest-api-issue-530-cover-all-60/12-04-SUMMARY.md` when done. Record final route count in the spec, the reconciliation-test approach used, and the information-disclosure review outcome.
</output>
