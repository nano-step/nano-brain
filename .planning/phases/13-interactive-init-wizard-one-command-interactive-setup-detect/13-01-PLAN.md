---
phase: 13-interactive-init-wizard-one-command-interactive-setup-detect
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/nano-brain/docker_provision.go
  - cmd/nano-brain/docker_provision_test.go
autonomous: true
requirements: [D-06, D-07]
must_haves:
  truths:
    - "docker info is classified into not-installed / daemon-down / available three ways"
    - "docker run name-conflict (exit 125) is recognized and recovered via docker start"
    - "docker run port-conflict (exit 125) is recognized, the stray Created container is docker rm'd, then retried on 5433"
    - "all docker CLI calls go through an injectable runDocker seam so tests never touch a real daemon"
  artifacts:
    - "cmd/nano-brain/docker_provision.go"
    - "cmd/nano-brain/docker_provision_test.go"
  key_links:
    - "runDocker var → exec.CommandContext(ctx, \"docker\", args...) (fixed argv, no user interpolation)"
    - "dockerStatus classification → wizard DB step branch selection (Plan 05 consumes)"
    - "provisionPostgres return (dbURL, err) → wizard writes database.url (Plan 05 consumes)"
---

<objective>
Add the `os/exec` Docker CLI wrapper that the DB step (Plan 05) will call to detect and provision a Postgres container. This is the only genuinely-new primitive in the phase besides the connect-poll loop.

Purpose: Implement D-06 (fixed `docker run` for `nanobrain-pg`/`pgvector/pgvector:pg17`) and D-07 (name-conflict → `docker start`; port-5432-conflict → `docker rm` stray + retry on 5433) behind a test seam, empirically matching the exit codes/stderr strings verified in RESEARCH.md.
Output: `cmd/nano-brain/docker_provision.go` (the runDocker seam, status classification, provision logic) + `cmd/nano-brain/docker_provision_test.go` (fake-runner table tests). No other file changes.
</objective>

<execution_context>
@/Users/tamlh/workspaces/self/AI/Tools/nano-brain/.claude/gsd-core/workflows/execute-plan.md
@/Users/tamlh/workspaces/self/AI/Tools/nano-brain/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-interactive-init-wizard-one-command-interactive-setup-detect/13-RESEARCH.md
@.planning/phases/13-interactive-init-wizard-one-command-interactive-setup-detect/13-PATTERNS.md
@CLAUDE.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: docker_provision_test.go — RED table tests for status classification and provisioning recovery</name>
  <read_first>
    - cmd/nano-brain/commands_test.go (lines 500-525: withRecoveryHooks save/override/t.Cleanup idiom — mirror this exactly for a runDocker override helper)
    - cmd/nano-brain/client.go (lines 18-33: the var runServeDaemonFn = runServeDaemon / isTTYFn hook-var idiom the runDocker var must copy)
    - .planning/phases/13-interactive-init-wizard-one-command-interactive-setup-detect/13-RESEARCH.md (Code Examples: VERIFIED docker exit codes/stderr; Common Pitfalls #1: stray Created container)
  </read_first>
  <behavior>
    - dockerStatus classification: runDocker returning (*exec.Error wrapping ErrNotFound) → dockerStatusNotInstalled; exitCode!=0 + stderr contains "Cannot connect to the Docker daemon" → dockerStatusDaemonNotRunning; exitCode==0 → dockerStatusAvailable; exitCode!=0 with other stderr → dockerStatusUnknownError
    - provisionPostgres success path: runDocker("run",...) returns exitCode 0 → returns the :5432 dbURL, nil error, no docker start / rm calls made
    - provisionPostgres name-conflict path: first "run" returns exit 125 + stderr containing "is already in use by container" → provision issues docker start nanobrain-pg (exit 0) → returns :5432 dbURL, nil
    - provisionPostgres port-conflict path: first "run" (port 5432) returns exit 125 + stderr containing "port is already allocated" → provision issues docker rm nanobrain-pg (stray cleanup, ignoring not-found) THEN a second "run" on :5433 (exit 0) → returns the :5433 dbURL, nil
    - Test asserts the exact docker subcommand + args sequence recorded by the fake runDocker for each path (e.g. port-conflict path records: run(-p 5432:5432...), rm, run(-p 5433:5433...))
  </behavior>
  <action>
    Create cmd/nano-brain/docker_provision_test.go in package main, table-driven per internal/health/doctor/doctor_test.go style. Declare a test helper withDockerRunner(t, script) that saves the package-level runDocker var, replaces it with a fake that pops scripted (stdout, stderr, exitCode, err) responses in order while recording each invocation's args, and restores via t.Cleanup — mirroring withRecoveryHooks at commands_test.go:500. Reference the four dockerStatus constants (dockerStatusNotInstalled, dockerStatusDaemonNotRunning, dockerStatusAvailable, dockerStatusUnknownError) and the two functions under test (dockerStatus(ctx) and provisionPostgres(ctx)) that Task 2 implements — the test file is written first and MUST fail to compile / fail assertions until Task 2 lands. Use the exact stderr substrings from RESEARCH.md Code Examples ("Cannot connect to the Docker daemon", "is already in use by container", "port is already allocated"). Construct the not-installed case by having the fake return a real *exec.Error wrapping exec.ErrNotFound (not a nil err with nonzero exit). Assert the recorded arg sequence for the port-conflict recovery includes a docker rm between the two runs. Do NOT run a real docker daemon. Commit RED per project pre-commit rule: because the harness-check.sh pre-commit hook blocks red commits, stage test + a minimal compiling stub together if needed, or capture the RED failure via a stash round-trip as done in Phase 999.1-01 — follow whichever the executor confirms the hook requires; the acceptance bar is that the test exists and exercises all four paths.
  </action>
  <verify>
    <automated>cd /Users/tamlh/workspaces/self/AI/Tools/nano-brain && go test -race -short ./cmd/nano-brain/ -run 'TestDockerStatus|TestProvisionPostgres' 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - go test compiles docker_provision_test.go (once Task 2 lands) and TestDockerStatus + TestProvisionPostgres run
    - Test source references all four dockerStatus* constants and both dockerStatus / provisionPostgres functions
    - Test source contains the literal substrings "is already in use by container" and "port is already allocated" as fake stderr inputs
    - Port-conflict test asserts a docker rm invocation is recorded between the two docker run invocations
    - No test invokes a real docker binary (grep the test file: no exec.Command("docker" outside the runDocker default, which is not referenced from tests)
  </acceptance_criteria>
  <done>docker_provision_test.go exists exercising status classification (4 cases) and provisioning recovery (success, name-conflict, port-conflict), using only the injected runDocker fake.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: docker_provision.go — runDocker seam, status classification, provisionPostgres (D-06/D-07)</name>
  <read_first>
    - cmd/nano-brain/docker_provision_test.go (the RED tests from Task 1 — the contract to satisfy)
    - cmd/nano-brain/daemon.go (lines 65-141: os.StartProcess exit-guard "check-state → early-return" shape to mirror for run/start/rm branching)
    - .planning/phases/13-interactive-init-wizard-one-command-interactive-setup-detect/13-RESEARCH.md (Architecture Patterns 1 & 2: dockerRunner type + defaultRunDocker body + daemon-vs-binary detection)
    - .planning/phases/13-interactive-init-wizard-one-command-interactive-setup-detect/13-CONTEXT.md (D-06 exact container spec, D-07 recovery rules)
  </read_first>
  <action>
    Create cmd/nano-brain/docker_provision.go in package main. Declare type dockerRunner func(ctx context.Context, args ...string) (stdout, stderr string, exitCode int, err error) and var runDocker dockerRunner = defaultRunDocker, where defaultRunDocker uses exec.CommandContext(ctx, "docker", args...) with a fixed argv (NEVER a shell string, NEVER user-interpolated args) capturing stdout/stderr into bytes.Buffer and extracting exitCode from *exec.ExitError, returning (-1 + the error) when the binary itself is missing (*exec.Error). Declare four string/int constants dockerStatusNotInstalled, dockerStatusDaemonNotRunning, dockerStatusAvailable, dockerStatusUnknownError. Implement dockerStatus(ctx) that runs runDocker(ctx, "info") and classifies per RESEARCH Pattern 2: err (binary missing) → NotInstalled; exitCode!=0 + stderr contains "Cannot connect to the Docker daemon" → DaemonNotRunning; other nonzero → UnknownError; zero → Available. Implement provisionPostgres(ctx) returning (dbURL string, err error): issue the fixed D-06 command runDocker(ctx, "run","-d","--name","nanobrain-pg","--restart","unless-stopped","-p","5432:5432","-e","POSTGRES_USER=nanobrain","-e","POSTGRES_PASSWORD=nanobrain","-e","POSTGRES_DB=nanobrain_dev","pgvector/pgvector:pg17"); on exit 0 return "postgres://nanobrain:nanobrain@localhost:5432/nanobrain_dev?sslmode=disable"; on exit 125 with stderr containing "is already in use by container" run docker start nanobrain-pg and on its exit 0 return the :5432 URL; on exit 125 with stderr containing "port is already allocated" run docker rm nanobrain-pg (ignore any not-found error per Pitfall 1), then retry docker run with -p 5433:5433 and on exit 0 return the :5433 URL. Return a descriptive error for any unrecovered exit. The container name is the fixed literal nanobrain-pg and the image is the fixed literal pgvector/pgvector:pg17 — no user value reaches the argv (V5 input-validation / command-injection mitigation). Keep this file free of any prompt/UI code — DB-step prompting lives in Plan 05.
  </action>
  <verify>
    <automated>cd /Users/tamlh/workspaces/self/AI/Tools/nano-brain && go build ./cmd/nano-brain/ && go test -race -short ./cmd/nano-brain/ -run 'TestDockerStatus|TestProvisionPostgres' 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - go build ./cmd/nano-brain/ succeeds
    - TestDockerStatus and TestProvisionPostgres pass (GREEN)
    - grep -n 'exec.CommandContext' cmd/nano-brain/docker_provision.go shows the fixed literal "docker" as command name
    - The docker run argv in source contains the exact tokens --name nanobrain-pg and pgvector/pgvector:pg17 and -p 5432:5432 (matching SETUP_AGENT.md Step 4)
    - The :5433 fallback URL and the :5432 URL both appear as sslmode=disable postgres:// literals; the port-conflict branch issues docker rm before the second run
    - No prompt/scanner/fmt.Scan usage in docker_provision.go (grep: no bufio, no promptWithDefault)
  </acceptance_criteria>
  <done>provisionPostgres and dockerStatus implemented against the fixed D-06 command with D-07 name/port recovery; RED tests from Task 1 pass; file is UI-free and injection-safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wizard → docker CLI (os/exec) | Wizard constructs a docker argv; only the phase's own fixed literals cross it in this plan (no user input) |
| docker CLI → local Docker daemon | Container lifecycle side effects on the user's own machine |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Tampering | provisionPostgres / defaultRunDocker argv | high | mitigate | exec.CommandContext(ctx, "docker", args...) with a fixed argv slice — the container name (nanobrain-pg) and image (pgvector/pgvector:pg17) are hardcoded literals; no user-entered value is interpolated into the argv in this plan, so no shell metacharacter can reach docker |
| T-13-02 | Denial of Service | port-conflict retry loop | low | accept | Retry is bounded to a single :5433 fallback after one docker rm; no unbounded loop. Local CLI action initiated by the operator |
| T-13-SC | Tampering | npm/pip/cargo installs | high | mitigate | N/A — this phase adds ZERO new packages (RESEARCH Package Legitimacy Audit: os/exec is stdlib, pgx/koanf already in go.mod). No install task exists; no legitimacy checkpoint required |
</threat_model>

<verification>
- `go build ./cmd/nano-brain/` succeeds (whole package still compiles)
- `go test -race -short ./cmd/nano-brain/ -run 'TestDockerStatus|TestProvisionPostgres'` passes
- No new go.mod entries (`git diff go.mod` empty)
</verification>

<success_criteria>
Docker detection is classified three ways and provisioning recovers from both name-conflict (docker start) and port-conflict (docker rm + retry on 5433), all behind the injectable runDocker seam, with zero real-daemon dependency in tests and no user input reaching the docker argv.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

New file `cmd/nano-brain/docker_provision.go` (package main):
- `type dockerRunner func(ctx context.Context, args ...string) (stdout, stderr string, exitCode int, err error)`
- `var runDocker dockerRunner = defaultRunDocker` (test seam)
- `func defaultRunDocker(ctx context.Context, args ...string) (string, string, int, error)`
- consts `dockerStatusNotInstalled`, `dockerStatusDaemonNotRunning`, `dockerStatusAvailable`, `dockerStatusUnknownError`
- `func dockerStatus(ctx context.Context) <statusType>`
- `func provisionPostgres(ctx context.Context) (dbURL string, err error)` — returns `postgres://nanobrain:nanobrain@localhost:5432/nanobrain_dev?sslmode=disable` or the `:5433` variant

New file `cmd/nano-brain/docker_provision_test.go`: `withDockerRunner` fake-seam helper, `TestDockerStatus`, `TestProvisionPostgres`.
</artifacts_produced>

<output>
Create `.planning/phases/13-interactive-init-wizard-one-command-interactive-setup-detect/13-01-SUMMARY.md` when done.
</output>
