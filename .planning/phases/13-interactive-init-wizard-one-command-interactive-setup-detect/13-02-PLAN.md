---
phase: 13-interactive-init-wizard-one-command-interactive-setup-detect
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/health/doctor/doctor.go
  - internal/health/doctor/doctor_test.go
autonomous: true
requirements: [D-13]
must_haves:
  truths:
    - "When embedding.provider is empty, doctor's provider check returns skip (not fail), detail 'disabled — BM25-only'"
    - "When embedding.provider is empty, doctor's model check returns skip (not fail)"
    - "When a provider IS configured (ollama/voyage), both checks behave exactly as before this change"
  artifacts:
    - "internal/health/doctor/doctor.go (edited CheckEmbeddingProvider + CheckEmbeddingModel)"
    - "internal/health/doctor/doctor_test.go (new disabled-provider cases)"
  key_links:
    - "cfg.Embedding.Provider == \"\" → early skip return before the existing ollama fallback"
    - "doctor skip status → wizard serve step treats BM25-only as passing (Plan 06 consumes)"
---

<objective>
Make `doctor` recognize `embedding.provider: ""` (D-11's BM25-only choice) as an intentional skip instead of a false FAIL against a nonexistent Ollama.

Purpose: Implement D-13 — the ONLY doctor behavior change in this phase. Without it, a user who disables embeddings gets a red FAIL that would (per D-14) also block the wizard's server-start step. This plan is a small, isolated, high-leverage edit that Plan 06's serve step depends on.
Output: Early `Provider == ""` skip guards in `CheckEmbeddingProvider` and `CheckEmbeddingModel`, plus table tests. No other file changes.
</objective>

<execution_context>
@/Users/tamlh/workspaces/self/AI/Tools/nano-brain/.claude/gsd-core/workflows/execute-plan.md
@/Users/tamlh/workspaces/self/AI/Tools/nano-brain/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-interactive-init-wizard-one-command-interactive-setup-detect/13-RESEARCH.md
@.planning/phases/13-interactive-init-wizard-one-command-interactive-setup-detect/13-PATTERNS.md
@CLAUDE.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: doctor_test.go — disabled-provider skip cases (RED)</name>
  <read_first>
    - internal/health/doctor/doctor_test.go (existing table-driven Check tests — TestCheckBinaryExists_*, TestCheckQueueHealth_* — mirror their Check{Status,...} field-assertion style)
    - internal/health/doctor/doctor.go (lines 100-176: current CheckEmbeddingProvider and CheckEmbeddingModel, including the Provider=="" → "ollama" fallback at 109-111 and the voyage/ollamaBody branches)
    - .planning/phases/13-interactive-init-wizard-one-command-interactive-setup-detect/13-CONTEXT.md (D-13 exact wording: skip, detail "disabled — BM25-only")
  </read_first>
  <behavior>
    - CheckEmbeddingProvider(config.EmbeddingConfig{Provider: ""}) returns Check.Status == "skip" and Check.Detail == "disabled — BM25-only", and returns a nil ollamaBody
    - CheckEmbeddingModel(config.EmbeddingConfig{Provider: ""}, nil) returns Check.Status == "skip"
    - CheckEmbeddingProvider(config.EmbeddingConfig{Provider: "voyage", VoyageAPIKey: "k"}) still returns Status "ok" (regression guard — configured provider path unchanged)
  </behavior>
  <action>
    Add TestCheckEmbeddingProvider_Disabled and TestCheckEmbeddingModel_Disabled to internal/health/doctor/doctor_test.go (package doctor_test). Assert exact Status "skip" and, for the provider check, Detail equal to the string "disabled — BM25-only". Add a TestCheckEmbeddingProvider_VoyageConfigured regression case asserting Status "ok" when Provider "voyage" + VoyageAPIKey set, so the change is proven not to alter the configured path. Import github.com/nano-brain/nano-brain/internal/config for the EmbeddingConfig literal. These tests reference the post-change behavior and must fail until Task 2 lands (commit RED per project hook rule — stage with a minimal change or stash-round-trip as needed).
  </action>
  <verify>
    <automated>cd /Users/tamlh/workspaces/self/AI/Tools/nano-brain && go test -race -short ./internal/health/doctor/ -run 'TestCheckEmbedding' 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Test file references config.EmbeddingConfig{Provider: ""} and asserts Status == "skip"
    - A test asserts the provider-check Detail equals the literal "disabled — BM25-only"
    - A regression test asserts Provider "voyage" + key still yields Status "ok"
    - `go test ... -run TestCheckEmbedding` executes these three cases (they fail pre-Task-2, pass post-Task-2)
  </acceptance_criteria>
  <done>doctor_test.go contains disabled-provider skip assertions for both checks plus a voyage-configured regression guard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: doctor.go — insert Provider=="" skip guards (D-13, GREEN)</name>
  <read_first>
    - internal/health/doctor/doctor.go (lines 100-176 — the exact functions to edit)
    - internal/health/doctor/doctor_test.go (the RED tests from Task 1 — the contract)
    - .planning/phases/13-interactive-init-wizard-one-command-interactive-setup-detect/13-PATTERNS.md (doctor "skip" status convention section)
  </read_first>
  <action>
    In CheckEmbeddingProvider, insert as the FIRST statement (before the existing `if cfg.Provider == "voyage"` branch and before the `if cfg.Provider == "" { cfg.Provider = "ollama" }` fallback at line 109) a guard: when cfg.Provider == "", return Check{Name: "Embedding provider", Status: "skip", Detail: "disabled — BM25-only"} together with a nil ollamaBody. In CheckEmbeddingModel, insert as the FIRST statement a matching guard: when cfg.Provider == "", return Check{Name: "Embedding model", Status: "skip", Detail: "disabled — BM25-only"}. Do not alter any other line in either function — the voyage path, the ollama fallback for a non-empty non-voyage provider, the ollamaBody-nil skip, and RunAll all stay exactly as-is (D-13: this is the only doctor behavior change, and only for the empty-provider case). The Detail string "disabled — BM25-only" must match the test assertion verbatim.
  </action>
  <verify>
    <automated>cd /Users/tamlh/workspaces/self/AI/Tools/nano-brain && go test -race -short ./internal/health/doctor/... 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race -short ./internal/health/doctor/...` fully green (new + all pre-existing cases)
    - grep -n 'disabled — BM25-only' internal/health/doctor/doctor.go returns two lines (provider + model guards)
    - The empty-provider guard appears before line 109's `cfg.Provider = "ollama"` fallback in CheckEmbeddingProvider (skip returns before any Ollama HTTP call)
    - Pre-existing tests (TestCheckQueueHealth_*, TestCheckBinaryExists_*, and any embedding-provider ok/fail tests) still pass unchanged
  </acceptance_criteria>
  <done>Both embedding checks skip cleanly when provider is empty; configured-provider behavior is byte-for-byte unchanged; full doctor suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config file → doctor | doctor reads embedding.provider from config; no network egress when provider is empty (the point of this change) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-03 | Information Disclosure | CheckEmbeddingProvider skip path | low | accept | The skip returns before any HTTP request, so a disabled config performs zero network egress — strictly reduces surface vs. today's behavior (which dials a default Ollama). No credential is echoed |
| T-13-SC | Tampering | npm/pip/cargo installs | high | mitigate | N/A — zero new packages (RESEARCH Package Legitimacy Audit). No install task; no legitimacy checkpoint required |
</threat_model>

<verification>
- `go test -race -short ./internal/health/doctor/...` green
- `go build ./...` succeeds (no signature change: both functions keep their existing return types)
</verification>

<success_criteria>
`nano-brain doctor` reports SKIP (not FAIL) for embedding provider + model when `embedding.provider` is empty, while every configured-provider path behaves identically to before.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

Edited `internal/health/doctor/doctor.go`:
- `CheckEmbeddingProvider(cfg config.EmbeddingConfig) (Check, []byte)` — new leading `Provider == ""` skip guard (Status "skip", Detail "disabled — BM25-only")
- `CheckEmbeddingModel(cfg config.EmbeddingConfig, ollamaBody []byte) Check` — matching leading skip guard

Edited `internal/health/doctor/doctor_test.go`: `TestCheckEmbeddingProvider_Disabled`, `TestCheckEmbeddingModel_Disabled`, `TestCheckEmbeddingProvider_VoyageConfigured`.

Behavioral change for downstream: `doctor.RunAll` now emits a `skip` (not `fail`) embedding result when provider is empty — Plan 06's serve step relies on this to not treat BM25-only as a blocking failure.
</artifacts_produced>

<output>
Create `.planning/phases/13-interactive-init-wizard-one-command-interactive-setup-detect/13-02-SUMMARY.md` when done.
</output>
