---
phase: 13-interactive-init-wizard-one-command-interactive-setup-detect
plan: 03
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - cmd/nano-brain/init_db.go
  - cmd/nano-brain/init_db_test.go
autonomous: true
requirements: [D-05, D-08, D-09]
must_haves:
  truths:
    - "A reachable default Postgres URL is detected via connect+ping and used with zero DB questions"
    - "When Postgres is unreachable, docker status decides between Docker-provision-prompt and remote-URL-prompt"
    - "After provisioning, readiness is confirmed by a pgx connect+ping poll loop (not docker exec pg_isready)"
    - "A user-entered Postgres URL is live-validated before it is accepted, with a 'save anyway' escape hatch"
    - "No Postgres password is ever printed — only parsed host is shown"
  artifacts:
    - "cmd/nano-brain/init_db.go"
    - "cmd/nano-brain/init_db_test.go"
  key_links:
    - "provisionPostgres (Plan 01) → waitForPostgresReady poll before returning dbURL"
    - "stepDatabase return (dbURL, ok) → orchestrator writes database.url (Plan 07 consumes)"
    - "connectPostgresFn seam → injectable so tests avoid a real DB"
---

<objective>
Build the database detection/provisioning wizard step (D-05 detection order, D-08 readiness poll, D-09 user-URL validation) as a standalone, injectable step function that Plan 07's orchestrator calls.

Purpose: This is the DB half of the wizard. It composes Plan 01's `provisionPostgres`/`dockerStatus` with a connect-poll loop shaped like `waitForServerHealthy` and the connect+ping shape of `doctor.CheckPostgreSQL`, plus a validate-before-write user-URL prompt with an escape hatch.
Output: `cmd/nano-brain/init_db.go` (`waitForPostgresReady`, a `pingPostgres` seam, `stepDatabase`) + `cmd/nano-brain/init_db_test.go`. No other file changes.
</objective>

<execution_context>
@/Users/tamlh/workspaces/self/AI/Tools/nano-brain/.claude/gsd-core/workflows/execute-plan.md
@/Users/tamlh/workspaces/self/AI/Tools/nano-brain/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-interactive-init-wizard-one-command-interactive-setup-detect/13-RESEARCH.md
@.planning/phases/13-interactive-init-wizard-one-command-interactive-setup-detect/13-PATTERNS.md
@cmd/nano-brain/docker_provision.go
@CLAUDE.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: init_db_test.go — poll-loop, URL validation, and detection-branch tests (RED)</name>
  <read_first>
    - cmd/nano-brain/client_helpers.go (lines 85-118: waitForServerHealthy deadline/remaining/clamp math to mirror)
    - cmd/nano-brain/commands_test.go (lines 455-466: time.Since(start) timeout-assertion style; lines 500-525: hook-var save/override/Cleanup idiom)
    - cmd/nano-brain/mcp_client_config.go (lines 349-359 promptConsequential, 229-231 isAffirmative — the prompt helpers the step reuses)
    - .planning/phases/13-interactive-init-wizard-one-command-interactive-setup-detect/13-RESEARCH.md (Pattern 3 waitForPostgresReady; Validation Architecture: use an injected connectFn for pure unit testing; Pitfall 2 restart-once)
  </read_first>
  <behavior>
    - waitForPostgresReady returns nil quickly when the injected pingPostgres seam succeeds on the first attempt
    - waitForPostgresReady returns a timeout error after ~the configured timeout when the seam always reports connection-refused, and does NOT return before the deadline (time.Since assertion, mirroring TestWaitForServerHealthy_Timeout)
    - waitForPostgresReady survives a refuse-then-ready sequence: seam returns refused on attempt 1, ready on attempt 2 → returns nil (covers Pitfall 2)
    - stepDatabase, when the initial pingPostgres(defaultURL) succeeds, returns the default dbURL with zero prompts issued (no scanner reads)
    - stepDatabase, when initial ping fails AND injected dockerStatus reports NotInstalled/DaemonNotRunning, prompts for a remote URL; a valid (seam-accepted) entered URL is returned; an invalid one re-prompts, and typing the escape answer ("save anyway"/blank-accept as implemented) returns the entered URL with a warning path
    - No test connects to a real Postgres; all DB reachability goes through injected seams
  </behavior>
  <action>
    Create cmd/nano-brain/init_db_test.go in package main. Add a withPostgresPinger(t, script) helper that overrides a package-level pingPostgres seam var (returning scripted nil/refused errors) with t.Cleanup restore, matching withRecoveryHooks. For stepDatabase tests, inject a fake docker status (either by overriding the runDocker seam from Plan 01 to script `docker info` exit codes, or by overriding a dockerStatusFn var if the planner exposes one — prefer reusing runDocker to avoid a second seam) and feed prompt answers via a *bufio.Scanner over bytes.NewBufferString, exactly as mcp_client_config_test.go feeds promptMCPClientConfig. Assert: reachable-default path issues zero scanner reads (assert the scanner buffer is fully unconsumed); unreachable+no-docker path reaches the remote-URL prompt; invalid-then-escape path returns the entered URL. Use time.Since assertions for the timeout case. Reference the three functions Task 2 implements: waitForPostgresReady(ctx, dbURL, timeout, interval), pingPostgres (seam var), stepDatabase(scanner, defaultURL) (returns dbURL string + ok bool). Commit RED per project hook rule.
  </action>
  <verify>
    <automated>cd /Users/tamlh/workspaces/self/AI/Tools/nano-brain && go test -race -short ./cmd/nano-brain/ -run 'TestWaitForPostgresReady|TestStepDatabase' 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Tests reference waitForPostgresReady, the pingPostgres seam, and stepDatabase
    - A test proves the reachable-default path issues zero prompts (scanner input untouched)
    - A test proves refuse-then-ready returns nil (Pitfall 2 coverage)
    - A timeout test uses time.Since to prove it does not return before the deadline
    - No real pgx.Connect to a live DB in tests (grep: pgx.Connect appears only inside init_db.go's default seam, not in the test file)
  </acceptance_criteria>
  <done>init_db_test.go covers poll success, timeout, refuse-then-ready, reachable-default-no-prompt, and unreachable→remote-URL-with-escape paths via injected seams.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: init_db.go — connect-poll, ping seam, stepDatabase (D-05/D-08/D-09, GREEN)</name>
  <read_first>
    - cmd/nano-brain/init_db_test.go (RED contract from Task 1)
    - internal/health/doctor/doctor.go (lines 61-86 CheckPostgreSQL connect+ping+host-parse shape; lines 69-73 url.Parse host extraction to avoid leaking credentials)
    - cmd/nano-brain/client_helpers.go (lines 88-118 poll-loop deadline math)
    - cmd/nano-brain/docker_provision.go (Plan 01: dockerStatus + provisionPostgres to compose)
    - .planning/phases/13-interactive-init-wizard-one-command-interactive-setup-detect/13-CONTEXT.md (D-05 detection order, D-08 ~30s/500ms budget, D-09 validate + escape hatch)
  </read_first>
  <action>
    Create cmd/nano-brain/init_db.go in package main. Declare var pingPostgres = defaultPingPostgres where defaultPingPostgres(ctx, dbURL) does pgx.Connect + conn.Ping with a 2-3s per-attempt timeout mirroring doctor.CheckPostgreSQL (returns error on refuse/ping-fail, nil on ready). Implement waitForPostgresReady(ctx, dbURL string, timeout, interval time.Duration) error using the exact deadline/remaining/clamp structure of waitForServerHealthy, calling pingPostgres each iteration and treating any error as transient until the deadline; start the timeout clock only after the caller has already provisioned (per Assumption A1 — image-pull time is separate). Implement stepDatabase(scanner *bufio.Scanner, defaultURL string) (dbURL string, ok bool) executing D-05 order: (1) pingPostgres(defaultURL, 3s) succeeds → return defaultURL, true with zero prompts; (2) else call dockerStatus(ctx): if Available, promptConsequential "PostgreSQL not found. Start one via Docker with default settings?" default "Y" → on affirmative call provisionPostgres(ctx) then waitForPostgresReady(ctx, provisionedURL, 30s, 500ms) and return that URL; (3) if docker NotInstalled/DaemonNotRunning OR user declines, print install guidance (https://docs.docker.com/get-docker/, note pgvector requirement) and prompt for a remote URL, then live-validate it via pingPostgres before accepting — on failure print the error and re-prompt, offering a "save anyway" escape (empty/explicit escape answer) that returns the entered URL with a printed warning (D-09). When printing any host to the user, extract host via url.Parse (never print the full credentialed URL — never echo the password). Keep provisioning/docker specifics delegated to Plan 01's functions; this file owns detection order + poll + prompt orchestration only. Use promptConsequential + isAffirmative (from mcp_client_config.go) for the Docker-provision and save-anyway gates since they cause side effects.
  </action>
  <verify>
    <automated>cd /Users/tamlh/workspaces/self/AI/Tools/nano-brain && go build ./cmd/nano-brain/ && go test -race -short ./cmd/nano-brain/ -run 'TestWaitForPostgresReady|TestStepDatabase' 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - go build ./cmd/nano-brain/ succeeds; TestWaitForPostgresReady + TestStepDatabase pass
    - waitForPostgresReady reuses the deadline/remaining/clamp shape (grep: time.Until(deadline) present)
    - stepDatabase calls dockerStatus and provisionPostgres from docker_provision.go (grep confirms references)
    - Host printing uses url.Parse; grep shows NO fmt call that prints a raw postgres:// URL variable that could contain a password (only parsed .Host is printed)
    - The Docker-provision prompt and save-anyway prompt use promptConsequential (grep), not promptWithDefault
  </acceptance_criteria>
  <done>stepDatabase implements the full D-05 detection order with D-08 poll and D-09 validate+escape, composing Plan 01's docker functions, never echoing DB credentials; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → wizard (Postgres URL entry) | Operator types a DSN that may contain credentials |
| wizard → remote/local Postgres (pgx connect) | Live connect to a user-supplied host during validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-04 | Information Disclosure | stepDatabase host/error printing | high | mitigate | Only the url.Parse-extracted .Host is printed; the raw DSN (with password) is never written to stdout/stderr or logs — mirrors doctor.CheckPostgreSQL's host-only detail. Connect errors are surfaced without embedding the credentialed URL |
| T-13-05 | Tampering / SSRF-adjacent | pgx.Connect to user-supplied URL | low | accept | Deliberate local-admin action on the operator's own machine, 3s timeout, same class as existing doctor.CheckPostgreSQL. V5: parse with url.Parse and only proceed for postgres:// scheme before connecting |
| T-13-SC | Tampering | npm/pip/cargo installs | high | mitigate | N/A — zero new packages (RESEARCH Package Legitimacy Audit). No install task; no legitimacy checkpoint required |
</threat_model>

<verification>
- `go build ./cmd/nano-brain/` succeeds
- `go test -race -short ./cmd/nano-brain/ -run 'TestWaitForPostgresReady|TestStepDatabase'` green
- `go test -race -short ./cmd/nano-brain/...` green (no regression in sibling tests)
</verification>

<success_criteria>
The wizard's DB step detects a reachable Postgres with zero questions, provisions via Docker (Plan 01) with a readiness poll when possible, falls back to a validated remote-URL prompt otherwise, and never prints a DB password.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

New file `cmd/nano-brain/init_db.go` (package main):
- `var pingPostgres = defaultPingPostgres` (test seam)
- `func defaultPingPostgres(ctx context.Context, dbURL string) error`
- `func waitForPostgresReady(ctx context.Context, dbURL string, timeout, interval time.Duration) error`
- `func stepDatabase(scanner *bufio.Scanner, defaultURL string) (dbURL string, ok bool)`

New file `cmd/nano-brain/init_db_test.go`: `withPostgresPinger`, `TestWaitForPostgresReady`, `TestStepDatabase`.
</artifacts_produced>

<output>
Create `.planning/phases/13-interactive-init-wizard-one-command-interactive-setup-detect/13-03-SUMMARY.md` when done.
</output>
