---
phase: 13-interactive-init-wizard-one-command-interactive-setup-detect
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - cmd/nano-brain/init_embedding.go
  - cmd/nano-brain/init_embedding_test.go
autonomous: true
requirements: [D-11, D-12]
must_haves:
  truths:
    - "The wizard asks 'Enable semantic embeddings?' first; declining writes embedding.provider empty and prints a BM25-only note"
    - "Enabling auto-detects local Ollama; when found it confirms URL+model, when not found it prompts provider (ollama URL | voyage)"
    - "Entering a non-local Ollama URL prints a cloud-auth caveat hint (no new auth code)"
    - "The generated embedding YAML block is correct for each of: disabled, ollama, voyage"
  artifacts:
    - "cmd/nano-brain/init_embedding.go"
    - "cmd/nano-brain/init_embedding_test.go"
  key_links:
    - "detectOllama (existing) → found/not-found branch"
    - "stepEmbedding return (embBlock string) → orchestrator YAML assembly (Plan 07 consumes)"
---

<objective>
Extract and upgrade the embedding wizard step into a testable step function implementing D-11 (optional embeddings, BM25 degrade via `provider: ""`) and D-12 (Ollama auto-detect + any-Ollama-URL/voyage, plus a cloud-auth hint — no new provider code).

Purpose: Today the embedding logic is inline in `runInteractiveInit` (init.go:66-94) with no "enable?" gate and always assumes a provider. This step adds the D-11 gate and returns the exact YAML block for the resolved choice, so Plan 07's orchestrator just concatenates it.
Output: `cmd/nano-brain/init_embedding.go` (`stepEmbedding` returning the embedding YAML block) + `cmd/nano-brain/init_embedding_test.go`. No other file changes.
</objective>

<execution_context>
@/Users/tamlh/workspaces/self/AI/Tools/nano-brain/.claude/gsd-core/workflows/execute-plan.md
@/Users/tamlh/workspaces/self/AI/Tools/nano-brain/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-interactive-init-wizard-one-command-interactive-setup-detect/13-RESEARCH.md
@.planning/phases/13-interactive-init-wizard-one-command-interactive-setup-detect/13-PATTERNS.md
@cmd/nano-brain/init.go
@CLAUDE.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: init_embedding_test.go — enable gate, detect branches, YAML block, cloud caveat (RED)</name>
  <read_first>
    - cmd/nano-brain/init.go (lines 16-24 detectOllama; lines 66-94 the current embedding block being extracted; lines 255-267 promptWithDefault)
    - cmd/nano-brain/mcp_client_config_test.go (how a *bufio.Scanner over bytes.NewBufferString feeds a prompt-driven function — mirror for feeding answers)
    - cmd/nano-brain/detect_test.go (existing detect-helper test patterns)
    - .planning/phases/13-interactive-init-wizard-one-command-interactive-setup-detect/13-CONTEXT.md (D-11 exact note text intent, D-12 branches; Pitfall 6 cloud-auth hint)
  </read_first>
  <behavior>
    - stepEmbedding with a "no" answer to the enable gate returns an embedding YAML block whose provider value is the empty string (rendered as provider: "") and prints a BM25-only note; no further prompts are read
    - stepEmbedding with "yes" + an injected detectOllama that returns true confirms the default URL + nomic-embed-text and returns an ollama block containing the URL and model
    - stepEmbedding with "yes" + detectOllama false + user chooses provider "ollama" and enters a URL returns an ollama block with that URL
    - stepEmbedding with "yes" + user chooses "voyage" returns a voyage block (provider: voyage, model)
    - Entering a non-localhost/non-private Ollama URL causes a cloud-auth caveat line to be printed (assert the printed output contains the caveat)
  </behavior>
  <action>
    Create cmd/nano-brain/init_embedding_test.go in package main. Inject the Ollama detection via a package-level seam (introduce var detectOllamaFn = detectOllama in Task 2 and override it here with t.Cleanup restore) so tests never hit the network. Feed prompt answers with a *bufio.Scanner over bytes.NewBufferString. Capture printed output by having stepEmbedding write through an io.Writer parameter, OR by asserting on the returned block + a returned/print-captured caveat — prefer passing the scanner and returning (block string) while routing user-facing prints through fmt to os.Stdout captured via a test helper; if capturing stdout is awkward, have stepEmbedding accept an io.Writer for its notes and assert on that buffer. Assert the returned block for each of the four cases (disabled → contains `provider: ""`; ollama-detected; ollama-manual; voyage). Add a case entering "https://ollama.com" (or another non-local host) and assert the caveat text about hosted/cloud endpoints requiring an API key is emitted. Reference stepEmbedding and the detectOllamaFn seam that Task 2 adds. Commit RED per project hook rule.
  </action>
  <verify>
    <automated>cd /Users/tamlh/workspaces/self/AI/Tools/nano-brain && go test -race -short ./cmd/nano-brain/ -run 'TestStepEmbedding' 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Test references stepEmbedding and the detectOllamaFn seam
    - A case asserts the disabled block contains the empty-provider rendering provider: "" (double-quoted empty)
    - Cases assert distinct ollama-detected, ollama-manual, and voyage blocks
    - A case asserts a cloud-auth caveat is printed for a non-local Ollama URL
    - No real network call in tests (detectOllama is overridden via the seam; grep: no http.Get in the test file)
  </acceptance_criteria>
  <done>init_embedding_test.go covers all four resolution paths plus the cloud-URL caveat via an injected detection seam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: init_embedding.go — stepEmbedding with D-11 gate + D-12 branches (GREEN)</name>
  <read_first>
    - cmd/nano-brain/init_embedding_test.go (RED contract from Task 1)
    - cmd/nano-brain/init.go (lines 66-94 the exact block to extract; lines 82,93 the embBlock fmt.Sprintf YAML strings)
    - internal/config/config.go (lines 93-101 EmbeddingConfig fields — provider/url/model/concurrency)
    - .planning/phases/13-interactive-init-wizard-one-command-interactive-setup-detect/13-RESEARCH.md (koanf empty-string override VERIFIED — provider: "" is correct, no sentinel; Pitfall 6 caveat text guidance)
  </read_first>
  <action>
    Create cmd/nano-brain/init_embedding.go in package main. Add var detectOllamaFn = detectOllama (test seam). Implement stepEmbedding(scanner *bufio.Scanner, defaultURL, defaultModel string) (embBlock string) — signature may also take an io.Writer for notes if Task 1's tests need it; keep it consistent with the test. Logic: (1) D-11 gate — promptWithDefault "Enable semantic embeddings?" default "Y"; if the answer is negative (use the existing "n"/"N" convention / isAffirmative), print the one-line BM25-only note ("BM25 keyword search only — re-run nano-brain init to enable embeddings later") and return the block `embedding:\n  provider: ""\n` (double-quoted empty string so koanf overrides the default per the VERIFIED research — do NOT invent a `none` sentinel). (2) Enabled → call detectOllamaFn(defaultURL): if true, print detected, confirm URL via promptWithDefault(defaultURL) and model via promptWithDefault(defaultModel), build the ollama block matching init.go:93's shape (provider, url, model, concurrency default 3). (3) detectOllamaFn false → prompt "Embedding provider (ollama/voyage)"; for ollama prompt the URL (any Ollama-compatible URL) and model, build the ollama block; for voyage prompt the model (default voyage-3) and build the voyage block matching init.go:82's shape (provider voyage, model, concurrency). (4) After a user enters an ollama URL, if the host is non-localhost/non-private (parse with url.Parse; treat 127.0.0.1/localhost/private RFC1918 ranges as local), print the cloud-auth caveat from RESEARCH Pitfall 6 ("hosted/cloud Ollama-compatible endpoints often require an API key; nano-brain does not currently send an Authorization header — self-hosted or local Ollama endpoints are the tested path") — this is a printed hint only, add NO auth code and do NOT modify internal/embed. Reuse the existing embBlock fmt.Sprintf formats so downstream config parsing is unchanged.
  </action>
  <verify>
    <automated>cd /Users/tamlh/workspaces/self/AI/Tools/nano-brain && go build ./cmd/nano-brain/ && go test -race -short ./cmd/nano-brain/ -run 'TestStepEmbedding' 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - go build ./cmd/nano-brain/ succeeds; TestStepEmbedding passes
    - grep -n 'provider: ""' cmd/nano-brain/init_embedding.go shows the disabled block uses the double-quoted empty provider (no `none` sentinel; grep shows no "provider: none")
    - The voyage and ollama blocks match the field shape of init.go's embBlock formats (provider/url/model/concurrency)
    - stepEmbedding introduces detectOllamaFn seam and calls it (grep) rather than detectOllama directly
    - No edits to internal/embed/* (git diff shows only init_embedding.go + its test)
  </acceptance_criteria>
  <done>stepEmbedding gates on enable (D-11), auto-detects/prompts provider (D-12), emits correct YAML per choice, and prints a cloud-auth caveat for remote Ollama URLs — with zero new auth code; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → wizard (embedding URL / API key entry) | Operator may enter a Voyage API key or a remote Ollama URL |
| wizard → config file | Embedding config (possibly a Voyage key) written to config.yml |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-06 | Information Disclosure | Voyage API key in config.yml | medium | mitigate | The embedding block is written by Plan 07's orchestrator via the existing os.WriteFile 0600 + os.MkdirAll 0700 pattern; this plan only returns the block string and never logs the key. (Voyage key handling matches the existing init.go behavior — unchanged surface) |
| T-13-07 | Information Disclosure | cloud Ollama URL 401 confusion | low | accept | Documented as a printed caveat (Pitfall 6); no auth header is added this phase (explicitly out of scope). No secret is echoed |
| T-13-SC | Tampering | npm/pip/cargo installs | high | mitigate | N/A — zero new packages (RESEARCH Package Legitimacy Audit). No install task; no legitimacy checkpoint required |
</threat_model>

<verification>
- `go build ./cmd/nano-brain/` succeeds
- `go test -race -short ./cmd/nano-brain/ -run TestStepEmbedding` green
- `go test -race -short ./cmd/nano-brain/...` green (no regression)
</verification>

<success_criteria>
The embedding step asks the enable gate first, degrades to BM25 via `provider: ""` on decline, auto-detects or prompts a provider on accept, warns on non-local Ollama URLs, and returns a correct YAML block for every path — no new embedding provider code.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 04)

New file `cmd/nano-brain/init_embedding.go` (package main):
- `var detectOllamaFn = detectOllama` (test seam)
- `func stepEmbedding(scanner *bufio.Scanner, defaultURL, defaultModel string) (embBlock string)` (signature may include an io.Writer for notes, kept consistent with tests)

New file `cmd/nano-brain/init_embedding_test.go`: `TestStepEmbedding` covering disabled / ollama-detected / ollama-manual / voyage / cloud-caveat.
</artifacts_produced>

<output>
Create `.planning/phases/13-interactive-init-wizard-one-command-interactive-setup-detect/13-04-SUMMARY.md` when done.
</output>
