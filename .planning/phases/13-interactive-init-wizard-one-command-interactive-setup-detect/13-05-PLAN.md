---
phase: 13-interactive-init-wizard-one-command-interactive-setup-detect
plan: 05
type: execute
wave: 2
depends_on: []
files_modified:
  - cmd/nano-brain/init_register.go
  - cmd/nano-brain/commands.go
  - cmd/nano-brain/init_register_test.go
autonomous: true
requirements: [D-15]
must_haves:
  truths:
    - "The workspace-registration HTTP flow (POST /api/v1/init, parse result, MCP prompt, triggerInitBackground) is a single callable helper"
    - "runInitCmd's --root branch calls that helper and behaves exactly as before (no observable change)"
    - "The helper returns the workspace name/hash/root so the wizard's register step (Plan 07) can print the summary"
  artifacts:
    - "cmd/nano-brain/init_register.go"
    - "cmd/nano-brain/commands.go (--root branch calls the helper)"
    - "cmd/nano-brain/init_register_test.go"
  key_links:
    - "registerWorkspace helper → POST /api/v1/init → parsed result (name, hash, root)"
    - "helper → promptMCPClientConfig (D-16 reuse, unchanged) + triggerInitBackground"
    - "helper return → wizard register step summary (Plan 07 consumes)"
---

<objective>
Extract `runInitCmd`'s `--root` registration body into a reusable `registerWorkspace` helper (D-15) so both the existing `--root` HTTP path and the new wizard register step call the same code instead of duplicating it.

Purpose: RESEARCH Pitfall 5 (VERIFIED) confirms `runInitCmd` lives in `commands.go:14-132`, NOT init.go. The registration HTTP body build + response parse + MCP prompt chain + `triggerInitBackground` (commands.go:78-131) must become one helper. This is a behavior-preserving refactor that unblocks Plan 07's register step.
Output: `cmd/nano-brain/init_register.go` (the `registerWorkspace` helper) + `commands.go` edited so its `--root` branch calls it + `cmd/nano-brain/init_register_test.go`. No behavior change to the CLI.
</objective>

<execution_context>
@/Users/tamlh/workspaces/self/AI/Tools/nano-brain/.claude/gsd-core/workflows/execute-plan.md
@/Users/tamlh/workspaces/self/AI/Tools/nano-brain/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-interactive-init-wizard-one-command-interactive-setup-detect/13-RESEARCH.md
@.planning/phases/13-interactive-init-wizard-one-command-interactive-setup-detect/13-PATTERNS.md
@cmd/nano-brain/commands.go
@CLAUDE.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: init_register_test.go — behavior-preserving registration helper test (RED)</name>
  <read_first>
    - cmd/nano-brain/commands.go (lines 14-132 runInitCmd including the --root branch to extract; lines 134-161 triggerInitBackground)
    - cmd/nano-brain/commands_test.go (the existing TestInitCmdBuildsCorrectBody-style test that uses httptest.Server + NANO_BRAIN_HOST/PORT env to intercept /api/v1/init — mirror this to drive the helper)
    - cmd/nano-brain/client.go (doRequest/getBaseURL — the HTTP infra the helper reuses)
    - .planning/phases/13-interactive-init-wizard-one-command-interactive-setup-detect/13-CONTEXT.md (D-15 extraction intent)
  </read_first>
  <behavior>
    - registerWorkspace POSTs to /api/v1/init with body {"root_path": root} (and "workspace" only when provided), matching the current --root request body
    - registerWorkspace returns the parsed result (WorkspaceHash, RootPath, Name, AgentsSnippet) from the server's JSON response
    - When jsonFlag is true the helper prints the raw response and returns without prompting for MCP config (matching current runInitCmd --json behavior)
    - The helper only invokes the MCP prompt when shouldPromptMCPConfig(jsonFlag, tty) is true and Name is non-empty (the existing empty-name guard is preserved)
  </behavior>
  <action>
    Create cmd/nano-brain/init_register_test.go in package main. Stand up an httptest.Server that records the POST /api/v1/init request body and returns a canned JSON result ({"workspace_hash":"...","root_path":"...","name":"...","agents_snippet":"..."}), pointing the CLI at it via t.Setenv NANO_BRAIN_HOST/PORT exactly as commands_test.go does for TestInitCmdBuildsCorrectBody. Call registerWorkspace with a temp root, jsonFlag=false, and TTY forced false (so the MCP prompt is skipped) and assert: the recorded request body equals the expected {"root_path": root}; the returned struct carries the canned name/hash/root. Add a second call with jsonFlag=true asserting the raw response is emitted and no /api/v1/init MCP-prompt path runs. Reference the registerWorkspace signature Task 2 implements. Commit RED per project hook rule (this test fails to compile until Task 2 adds registerWorkspace).
  </action>
  <verify>
    <automated>cd /Users/tamlh/workspaces/self/AI/Tools/nano-brain && go test -race -short ./cmd/nano-brain/ -run 'TestRegisterWorkspace' 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Test references registerWorkspace and drives it through an httptest.Server intercepting /api/v1/init
    - A test asserts the POST body equals {"root_path": <root>} for the no-workspace case
    - A test asserts the returned struct exposes name + workspace_hash + root_path from the canned response
    - TTY is forced false in the non-json case so no interactive MCP prompt blocks the test
  </acceptance_criteria>
  <done>init_register_test.go exercises the extracted helper's request body, response parsing, and json-flag short-circuit through httptest.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: extract registerWorkspace into init_register.go and call it from commands.go (D-15, GREEN)</name>
  <read_first>
    - cmd/nano-brain/commands.go (lines 14-132 — the source of the extraction; keep the --root/--workspace/--json/--force flag parsing and the --force reset-workspace block in runInitCmd, move only the register body)
    - cmd/nano-brain/init_register_test.go (RED contract from Task 1)
    - cmd/nano-brain/mcp_client_config.go (shouldPromptMCPConfig, promptMCPClientConfig — reused verbatim, D-16)
    - .planning/phases/13-interactive-init-wizard-one-command-interactive-setup-detect/13-PATTERNS.md (init_register.go section: exact extraction target lines commands.go:78-131)
  </read_first>
  <action>
    Create cmd/nano-brain/init_register.go in package main. Define a struct type (e.g. initResult) with WorkspaceHash, RootPath, Name, AgentsSnippet json tags matching commands.go's current anonymous struct. Implement func registerWorkspace(root, workspace string, jsonFlag bool) (initResult, error): build body {"root_path": root} (+ "workspace" if non-empty), doRequest POST getBaseURL()+"/api/v1/init"; on jsonFlag print raw response and return (zero result, nil) after logging completion; else json.Unmarshal into initResult, print the "Workspace registered / Root path / AgentsSnippet" lines exactly as today, then if shouldPromptMCPConfig(jsonFlag, isTTY()) && result.Name != "" call promptMCPClientConfig(bufio.NewScanner(os.Stdin), result.RootPath, result.Name) (preserve the empty-name warning branch verbatim), then triggerInitBackground(result.WorkspaceHash, root), and return (result, nil). Then edit commands.go's runInitCmd --root branch to REPLACE the inlined body (current lines ~78-131, the marshal → doRequest → parse → MCP-prompt → triggerInitBackground section) with a call to registerWorkspace(root, workspace, jsonFlag), keeping the surrounding flag parsing and the --force reset-workspace block (lines 57-76) unchanged. Preserve the cliLog.Info completed logging. The observable CLI output and side effects of `nano-brain init --root <p>` must be identical before and after (behavior-preserving extraction). Keep triggerInitBackground where it is (commands.go) — same package, callable from the helper without moving it.
  </action>
  <verify>
    <automated>cd /Users/tamlh/workspaces/self/AI/Tools/nano-brain && go build ./cmd/nano-brain/ && go test -race -short ./cmd/nano-brain/ -run 'TestRegisterWorkspace|TestInitCmd' 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - go build ./cmd/nano-brain/ succeeds; TestRegisterWorkspace + any existing TestInitCmd* pass
    - grep -n 'registerWorkspace' cmd/nano-brain/commands.go shows the --root branch now calls the helper
    - commands.go no longer contains a second inlined POST to /api/v1/init inside runInitCmd (grep: the /api/v1/init POST for registration lives in init_register.go; the --force reset-workspace POST to /api/v1/reset-workspace remains in commands.go)
    - promptMCPClientConfig and triggerInitBackground are invoked from the helper; the empty-name warning branch is preserved (grep: the warning string still present)
    - Full cmd/nano-brain suite passes (`go test -race -short ./cmd/nano-brain/...`) — no regression to existing init behavior
  </acceptance_criteria>
  <done>registerWorkspace helper owns the registration flow; commands.go --root calls it; existing init CLI behavior is unchanged; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → API (/api/v1/init) | Registration request crosses to the local server; response drives MCP config writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-08 | Tampering | registerWorkspace MCP-prompt gate | medium | mitigate | The empty-name guard (skip MCP auto-config when server returns no name) is preserved verbatim so a stale server can't produce a broken ?workspace= binding; MCP writes still go through Phase 10's consent-gated promptMCPClientConfig (D-16, unchanged) |
| T-13-09 | Repudiation | behavior-preserving refactor | low | accept | Extraction is covered by a request-body + response-parse test plus the existing init suite, proving no observable behavior change |
| T-13-SC | Tampering | npm/pip/cargo installs | high | mitigate | N/A — zero new packages (RESEARCH Package Legitimacy Audit). No install task; no legitimacy checkpoint required |
</threat_model>

<verification>
- `go build ./cmd/nano-brain/` succeeds
- `go test -race -short ./cmd/nano-brain/ -run 'TestRegisterWorkspace|TestInitCmd'` green
- `go test -race -short ./cmd/nano-brain/...` green (existing init behavior preserved)
</verification>

<success_criteria>
The registration flow is a single `registerWorkspace` helper called by both `init --root` and (later) the wizard, with `init --root`'s CLI output and side effects unchanged.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 05)

New file `cmd/nano-brain/init_register.go` (package main):
- `type initResult struct { WorkspaceHash, RootPath, Name, AgentsSnippet string }` (json-tagged)
- `func registerWorkspace(root, workspace string, jsonFlag bool) (initResult, error)` — POSTs /api/v1/init, parses result, runs the D-16 MCP prompt (consent-gated, empty-name-guarded), calls triggerInitBackground

Edited `cmd/nano-brain/commands.go`: `runInitCmd`'s `--root` branch now calls `registerWorkspace(...)` instead of the inlined body; flag parsing and the `--force` reset-workspace block unchanged; `triggerInitBackground` stays in commands.go.

New file `cmd/nano-brain/init_register_test.go`: `TestRegisterWorkspace` (httptest-driven body/response/json-flag assertions).
</artifacts_produced>

<output>
Create `.planning/phases/13-interactive-init-wizard-one-command-interactive-setup-detect/13-05-SUMMARY.md` when done.
</output>
