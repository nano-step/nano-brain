---
phase: 13-interactive-init-wizard-one-command-interactive-setup-detect
plan: 07
type: execute
wave: 3
depends_on: ["13-03", "13-04", "13-05", "13-06"]
files_modified:
  - cmd/nano-brain/init.go
  - cmd/nano-brain/init_test.go
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-16, D-17]
must_haves:
  truths:
    - "runInteractiveInit returns immediately (no prompts) when not a TTY, preserving the D-04 non-interactive contract"
    - "When config already exists the wizard offers [k]eep/[o]verwrite (default keep); keep jumps straight to the service steps (doctor → serve → register → MCP) with zero config questions"
    - "The core happy path issues at most 6 consequential questions (D-01 budget); harvester/summarization/search/watcher/logging are silent defaults unless the advanced gate is accepted"
    - "The Advanced settings? [y/N] gate defaults to No and skips straight to config preview; Yes runs the existing detailed prompt sequence unchanged"
    - "The orchestrator composes stepDatabase, stepEmbedding, stepServe, registerWorkspace and promptMCPClientConfig in order; it does not re-implement their logic"
    - "The final summary block prints the server URL, workspace name/hash, which MCP clients were configured, and the single next action 'restart your AI client' (D-17)"
    - "The config file is written 0600 under a 0700 dir and no DB password or API key is echoed to stdout"
  artifacts:
    - "cmd/nano-brain/init.go (runInteractiveInit restructured into the step sequence + orchestrator seams)"
    - "cmd/nano-brain/init_test.go (TTY-gate, keep-path, question-budget, advanced-gate, summary tests)"
  key_links:
    - "isTTYFn() false → early return (D-04) before any prompt"
    - "stepConfigGate keep → skip stepDatabase/stepEmbedding/stepAdvanced/write, jump to doctor→serve→register→MCP (D-03)"
    - "doctor.RunAll(configPath, cfg, cfgErr, resolveBinaryPath()) → []doctor.Check → stepServe(scanner, checks, configPath) (Plan 06 gate)"
    - "stepDatabase (Plan 03) return dbURL → database.url in assembled YAML"
    - "stepEmbedding (Plan 04) return embBlock → embedding section in assembled YAML"
    - "stepServe (Plan 06) serveOutcome → register step only proceeds when server is reachable"
    - "registerWorkspace (Plan 05) initResult{Name,WorkspaceHash,RootPath} → promptMCPClientConfig(scanner, RootPath, Name) (D-16) → summary block (D-17)"
---

<objective>
Restructure `runInteractiveInit` (cmd/nano-brain/init.go) from the current ~20-question static prompt into a thin orchestrator that calls the Wave-1/Wave-2 step functions in order and chains all the way through to a running, registered, MCP-configured state — implementing D-01 (≤6 core questions), D-02 (advanced gate), D-03 (keep/overwrite gate), D-04 (TTY contract), D-16 (Phase 10 MCP prompt reuse), and D-17 (final summary block).

Purpose: Wave 1/2 built every load-bearing step in isolation (stepDatabase, stepEmbedding, registerWorkspace, stepServe, and the doctor skip change). This plan is the control-flow work that wires them into the single "one command" flow described in RESEARCH's System Architecture Diagram, replacing today's dead-end ("start the server and run init --root ... manually"). It composes; it does not re-implement.
Output: `cmd/nano-brain/init.go` with `runInteractiveInit` collapsed into the step sequence (config-exists gate → DB → embedding → advanced gate → write+doctor → serve → register → MCP → summary), the advanced prompts preserved verbatim behind the D-02 gate, and `cmd/nano-brain/init_test.go` covering the orchestration seams. No other file changes.
</objective>

<execution_context>
@/Users/tamlh/workspaces/self/AI/Tools/nano-brain/.claude/gsd-core/workflows/execute-plan.md
@/Users/tamlh/workspaces/self/AI/Tools/nano-brain/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-interactive-init-wizard-one-command-interactive-setup-detect/13-RESEARCH.md
@.planning/phases/13-interactive-init-wizard-one-command-interactive-setup-detect/13-PATTERNS.md
@.planning/phases/13-interactive-init-wizard-one-command-interactive-setup-detect/13-CONTEXT.md
@.planning/phases/13-interactive-init-wizard-one-command-interactive-setup-detect/13-03-SUMMARY.md
@.planning/phases/13-interactive-init-wizard-one-command-interactive-setup-detect/13-04-SUMMARY.md
@.planning/phases/13-interactive-init-wizard-one-command-interactive-setup-detect/13-05-SUMMARY.md
@.planning/phases/13-interactive-init-wizard-one-command-interactive-setup-detect/13-06-SUMMARY.md
@cmd/nano-brain/init.go
@cmd/nano-brain/mcp_client_config.go
@cmd/nano-brain/doctor.go
@CLAUDE.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: init_test.go — TTY gate, keep-path, question-budget, advanced-gate, summary (RED)</name>
  <read_first>
    - cmd/nano-brain/mcp_client_config_test.go (how a *bufio.Scanner over bytes.NewBufferString feeds a prompt-driven orchestrator; how tests assert on captured output)
    - cmd/nano-brain/commands_test.go (lines 500-525: the withRecoveryHooks save/override/t.Cleanup idiom for isTTYFn/promptReader/runServeDaemonFn — mirror it for the orchestrator seams)
    - cmd/nano-brain/init.go (current runInteractiveInit 26-253 — the flow being restructured; note the config-exists gate 41-49, the YAML assembly 214-244, the write 234-242)
    - cmd/nano-brain/mcp_client_config.go (lines 349-359 promptConsequential — EOF=decline; 229-231 isAffirmative)
    - .planning/phases/13-interactive-init-wizard-one-command-interactive-setup-detect/13-03-SUMMARY.md through 13-06-SUMMARY.md (the ACTUAL signatures/seams Wave 2 shipped — stepDatabase, stepEmbedding, registerWorkspace, stepServe, serveOutcome constants; reconcile any drift from the plan-level signatures before writing the test)
    - .planning/phases/13-interactive-init-wizard-one-command-interactive-setup-detect/13-CONTEXT.md (D-01 the six core questions, D-02 advanced gate wording, D-03 keep/overwrite, D-04 TTY, D-17 summary contents)
  </read_first>
  <behavior>
    - runInteractiveInit, with isTTYFn forced to return false, returns without reading any prompt input and without writing a config file (D-04 non-interactive contract)
    - With an existing config file and the user answering keep (Enter / "k") to the [k]eep/[o]verwrite gate, the wizard reads NO database/embedding/advanced prompts and does NOT rewrite the config file; it proceeds to the service steps (assert config mtime/content unchanged and that the DB/embedding step seams are never invoked) (D-03)
    - With no existing config (or overwrite chosen) and every step seam stubbed to a no-prompt happy path, the number of consequential prompts actually issued on the core path is ≤6 (assert by counting reads consumed from a scanner whose buffer holds a known number of lines, or by counting promptConsequential invocations via a seam counter) (D-01)
    - The Advanced settings? [y/N] gate: default (empty answer) does NOT run the harvester/summarization/search/watcher/logging prompts; answering y DOES run them (assert by detecting whether the advanced prompt block consumed its scanner lines) (D-02)
    - After a successful register (registerWorkspace seam returns initResult{Name:"proj", WorkspaceHash:"abc123", RootPath:"/tmp/x"}), the final summary output contains the server URL, the workspace name and hash, and the literal next-action phrase about restarting the AI client (D-17)
  </behavior>
  <action>
    Create cmd/nano-brain/init_test.go in package main. Add a hook helper (mirroring withRecoveryHooks at commands_test.go:500) that saves/overrides/restores the orchestrator seams Task 2 introduces: isTTYFn (existing), and NEW package-level function-var seams so the orchestrator's step calls are injectable — introduce in Task 2 e.g. stepDatabaseFn, stepEmbeddingFn, runDoctorChecksFn (returns []doctor.Check), stepServeFn, registerWorkspaceFn, promptMCPClientConfigFn — each defaulting to the real Wave-1/2 function. Override them in tests with fakes that record call counts and return canned values, so this orchestration test never touches Docker, a DB, the daemon, or the network. Feed prompt answers with a *bufio.Scanner over bytes.NewBufferString exactly as mcp_client_config_test.go does. Capture user-facing output for the summary/keep assertions via the same stdout-capture approach the sibling tests use (or an injected io.Writer if Task 2 threads one — keep it consistent with the sibling step tests). Write these cases: TestRunInteractiveInit_NonTTY (isTTYFn=false → zero reads, no file written; use a temp configPath and assert os.Stat still ErrNotExist); TestRunInteractiveInit_KeepExisting (write a sentinel config first, answer keep → assert file bytes unchanged and stepDatabaseFn/stepEmbeddingFn call count 0); TestRunInteractiveInit_QuestionBudget (fresh config, all seams happy → assert ≤6 consequential prompts consumed on the core path); TestRunInteractiveInit_AdvancedGate (assert default-N skips the advanced block and Y runs it); TestRunInteractiveInit_Summary (assert the summary text contains the server URL, workspace name, hash, and the restart-your-AI-client next action). Reference runInteractiveInit and the seam vars Task 2 adds. Commit RED per the project pre-commit hook rule (stage with the minimal compiling stub or the stash round-trip the executor confirms harness-check.sh requires).
  </action>
  <verify>
    <automated>cd /Users/tamlh/workspaces/self/AI/Tools/nano-brain && go test -race -short ./cmd/nano-brain/ -run 'TestRunInteractiveInit' 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - Test references runInteractiveInit and the orchestrator step seams (stepDatabaseFn/stepEmbeddingFn/stepServeFn/registerWorkspaceFn/promptMCPClientConfigFn or the names Task 2 lands)
    - A case forces isTTYFn=false and asserts no file is written and no scanner line is consumed (D-04)
    - A case proves keep skips DB/embedding/advanced and leaves the existing config bytes unchanged (D-03)
    - A case asserts the core path issues ≤6 consequential prompts (D-01)
    - A case asserts the advanced gate default-N skips the detailed prompts and Y runs them (D-02)
    - A case asserts the summary contains server URL + workspace name + hash + restart-AI-client next action (D-17)
    - No real Docker/DB/daemon/network in the test (all via seams; grep: no exec.Command, no pgx.Connect, no http.Get in init_test.go)
  </acceptance_criteria>
  <done>init_test.go drives the orchestrator through injected step seams and proves the TTY gate, keep-path, ≤6-question budget, advanced gate, and summary contents without any real side effects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: init.go — restructure runInteractiveInit into the step sequence (D-01..D-04, D-16, D-17, GREEN)</name>
  <read_first>
    - cmd/nano-brain/init_test.go (RED contract from Task 1)
    - cmd/nano-brain/init.go (the full current runInteractiveInit — the advanced prompt block 96-212 to preserve verbatim behind the D-02 gate; the YAML assembly 214-244; the config-exists gate 41-49; keep promptWithDefault 255-267 as-is)
    - cmd/nano-brain/doctor.go (lines 13-31: runDoctorCmd's config.Load + doctor.RunAll(configPath, cfg, cfgErr, resolveBinaryPath()) call — the wizard must call doctor.RunAll DIRECTLY and print results itself, NOT call runDoctorCmd, because runDoctorCmd os.Exit(1)s on any fail and would kill the wizard before the serve gate)
    - cmd/nano-brain/mcp_client_config.go (lines 221-231 shouldPromptMCPConfig/isAffirmative; 244-279 promptMCPClientConfig; 349-359 promptConsequential)
    - .planning/phases/13-interactive-init-wizard-one-command-interactive-setup-detect/13-03-SUMMARY.md..13-06-SUMMARY.md (the shipped stepDatabase/stepEmbedding/registerWorkspace/stepServe signatures + serveOutcome constants to call — use the ACTUAL signatures, not the plan-level ones if they drifted)
    - .planning/phases/13-interactive-init-wizard-one-command-interactive-setup-detect/13-RESEARCH.md (lines 117-183 System Architecture Diagram — the exact step order and branch behavior)
    - cmd/nano-brain/client.go (line 29 isTTYFn; line 71 getBaseURL for the summary server URL; line 35 serverHealthTimeout)
  </read_first>
  <action>
    Restructure runInteractiveInit in cmd/nano-brain/init.go into a thin orchestrator following the RESEARCH System Architecture Diagram. Concretely:

    (0) TTY gate (D-04): as the FIRST statement, if !isTTYFn() print a one-line note that interactive init needs a TTY (point non-interactive callers at `nano-brain init --root <path> --json`) and return — no prompts, no file writes.

    (1) Introduce injectable orchestrator seams as package-level function vars, each defaulting to the real Wave-1/2 function, so Task 1's test can stub them: e.g. `var stepDatabaseFn = stepDatabase`, `var stepEmbeddingFn = stepEmbedding`, `var stepServeFn = stepServe`, `var registerWorkspaceFn = registerWorkspace`, `var promptMCPClientConfigFn = promptMCPClientConfig`, and a `var runDoctorChecksFn = defaultRunDoctorChecks` where defaultRunDoctorChecks(configPath) loads config via config.Load and returns doctor.RunAll(configPath, cfg, cfgErr, resolveBinaryPath()) plus prints the same per-check table runDoctorCmd prints (extract the print loop or call a shared printer — do NOT call runDoctorCmd itself because it os.Exit(1)s). Match the seam-var idiom used by runServeDaemonFn/isTTYFn in client.go:29 exactly.

    (2) Config-exists gate (D-03): if os.Stat(configPath) succeeds, prompt "[k]eep/[o]verwrite" default keep via promptConsequential (consequential — EOF must not silently overwrite). On keep (default / "k" / "keep"): set a `keep := true` flag, print a "keeping existing config" note, and SKIP the database, embedding, advanced, and write steps entirely — jump straight to (7) doctor. On overwrite ("o"/"overwrite"): fall through to the config-building steps. Reuse the gate shape from the current lines 41-49 but change the prompt and default per D-03 (default is KEEP now, not overwrite).

    (3) Database step: dbURL, ok := stepDatabaseFn(scanner, defaultDBURL). If !ok, print an abort note and return (the user declined / could not resolve a DB). This is core question #2 (only asked when the default URL is unreachable — stepDatabase owns that branch).

    (4) Embedding step: embBlock := stepEmbeddingFn(scanner, defaultEmbURL, defaultEmbModel) (use the ACTUAL shipped signature from 13-04-SUMMARY, including any io.Writer). This is core question #3 (enable y/n + provider details).

    (5) Advanced gate (D-02): promptConsequential "Advanced settings?" default "N". On a non-affirmative (default) answer, build the config with SILENT DEFAULTS for harvester/summarization/search/watcher/logging (auto-detect harvester dirs via the existing detectOpenCodeStorageDir/detectClaudeCodeStorageDir with no prompt; summarization disabled; search/watcher/logging defaults) — reuse the exact default block strings from the current init.go. On an affirmative answer, run the existing detailed prompt sequence (current lines 96-212) VERBATIM — move that block, unchanged, into a `stepAdvanced`/inline branch so the functionality is preserved, just gated. Do NOT delete or simplify any advanced prompt (no scope reduction — the advanced path must be byte-for-byte the current behavior).

    (6) Assemble + preview + write: reuse the current YAML assembly (lines 214-222) and the MkdirAll 0700 + WriteFile 0600 write (lines 234-242) unchanged, feeding it dbURL and embBlock from the steps and the default-or-advanced blocks. Keep the "Save this config?" confirm. This is not counted as one of the six DB/embedding/serve/register/MCP core questions — it is the write confirm the current flow already has; keep it, but ensure the core-question budget (D-01) counts the six consequential decisions listed in CONTEXT D-01 (overwrite/keep, database, embeddings, start server, register, per-MCP-client). The advanced-gate Y/N is the D-02 gate, allowed on top of the six.

    (7) Doctor: results := runDoctorChecksFn(configPath) — prints the check table and returns []doctor.Check. (Because of Plan 02, a disabled embedding provider yields skip, not fail.)

    (8) Serve step (D-14, Plan 06): outcome := stepServeFn(scanner, results, configPath). This is core question #4 ("Start server now?"). If outcome is serveAborted (doctor PostgreSQL FAIL) print the fix hint already emitted by stepServe and return non-zero (os.Exit(1)) — do not continue to register against a dead server.

    (9) Register step (D-15, Plan 05): only when the server is reachable (outcome serveStarted or serveAlreadyRunning). Prompt "Register this directory as a workspace?" default the cwd via promptConsequential (core question #5); on accept call res, err := registerWorkspaceFn(root, "", false) and capture res for the summary; on a decline/skip, note that MCP config needs a workspace and skip to summary. registerWorkspace itself (Plan 05) runs the D-16 promptMCPClientConfig internally (per its shipped contract) — so do NOT double-invoke promptMCPClientConfig here; the per-MCP-client Y/N prompts (core question #6) happen inside registerWorkspace. (If 13-05-SUMMARY shows registerWorkspace does NOT call promptMCPClientConfig, then call promptMCPClientConfigFn(scanner, res.RootPath, res.Name) here once, guarded by res.Name != "" — reconcile against the actual shipped behavior so MCP config runs exactly once.)

    (10) Summary block (D-17): print a short block containing the server URL (getBaseURL()), the workspace name and hash (res.Name / res.WorkspaceHash when a registration happened), which MCP clients were configured (from what registerWorkspace/promptMCPClientConfig reported — if that info isn't returned, print the generic "MCP clients you selected above"), and the single next action: restart your AI client. Never echo the DB password or any API key in the summary (print getBaseURL()/host only, never the database.url with credentials).

    Keep promptWithDefault unchanged. Add NO new external package. The orchestrator must be small — the heavy logic lives in the step files from Waves 1/2. Ensure the file still builds under `go build ./cmd/nano-brain/`.
  </action>
  <verify>
    <automated>cd /Users/tamlh/workspaces/self/AI/Tools/nano-brain && go build ./cmd/nano-brain/ && go test -race -short ./cmd/nano-brain/ -run 'TestRunInteractiveInit' 2>&1 | tail -25 && echo "--- credential-echo guard: no raw database.url printed ---" && grep -n 'fmt.Print' cmd/nano-brain/init.go | grep -i 'dburl\|database.url\|password' | grep -v '^#' | wc -l | sed 's/^/raw-credential prints (must be 0): /'</automated>
  </verify>
  <acceptance_criteria>
    - go build ./cmd/nano-brain/ succeeds; TestRunInteractiveInit* pass GREEN; full `go test -race -short ./cmd/nano-brain/...` green
    - runInteractiveInit's first statement is the isTTYFn() gate returning early (grep: isTTYFn appears before any scanner/promptConsequential call in the function)
    - The orchestrator calls stepDatabaseFn/stepEmbeddingFn/stepServeFn/registerWorkspaceFn (grep confirms the seam calls) rather than re-implementing DB/embedding/serve/register logic inline
    - runInteractiveInit does NOT call runDoctorCmd (grep: no runDoctorCmd reference in init.go) — it uses runDoctorChecksFn/doctor.RunAll directly so a doctor FAIL does not os.Exit before the serve gate
    - The keep branch of the config-exists gate skips the DB/embedding/advanced/write steps (proven by Task 1's keep test)
    - The advanced prompt block (current init.go lines 96-212) is preserved (grep: the harvester/summarization/search/watcher/logging prompt strings still present, now under the D-02 gate) — no advanced functionality removed
    - MCP config (D-16 promptMCPClientConfig) runs exactly once on the register path (not double-invoked)
    - The summary prints getBaseURL()/host, never a raw credentialed postgres:// URL (verify grep count is 0)
    - No new go.mod entry (`git diff go.mod` empty)
  </acceptance_criteria>
  <done>runInteractiveInit is a thin TTY-gated orchestrator: keep/overwrite gate → DB → embedding → advanced gate → write+doctor → serve → register → MCP → summary, composing the Wave-1/2 steps, honoring the ≤6-question budget, preserving the advanced prompts behind the D-02 gate, and never echoing credentials; tests green and no new packages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → wizard (TTY prompts) | Operator drives the whole setup flow interactively; EOF/closed stdin must not cause unconsented writes or process starts |
| wizard → config file (~/.nano-brain/config.yml) | Assembled YAML (may contain DB DSN / Voyage key) written to disk |
| wizard → child steps (daemon start, registration POST, MCP config writes) | The orchestrator triggers consequential side effects owned by the step files |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-12 | Tampering | config write permissions | high | mitigate | The orchestrator reuses the existing os.MkdirAll(dir, 0700) + os.WriteFile(configPath, ..., 0600) pattern unchanged (init.go:234-242) — the config carrying the DB DSN / API key is never world-readable |
| T-13-13 | Information Disclosure | summary / prompts echoing credentials | high | mitigate | The D-17 summary and all orchestrator prints emit only getBaseURL()/host and the workspace name/hash — never the raw database.url (which may embed a password) nor any API key; verified by the credential-echo grep gate |
| T-13-14 | Tampering / Elevation | unconsented write or daemon start on EOF | medium | mitigate | Every consequential gate (keep/overwrite, save config, advanced, start server, register) uses promptConsequential where EOF/closed-stdin means DECLINE (CR-01), and the isTTYFn gate (D-04) returns before any prompt in a non-interactive context — no silent write/start |
| T-13-SC | Tampering | npm/pip/cargo installs | high | mitigate | N/A — zero new packages (RESEARCH Package Legitimacy Audit: this phase adds no external dependency). No install task; no legitimacy checkpoint required |
</threat_model>

<verification>
- `go build ./cmd/nano-brain/` (native) succeeds
- `go test -race -short ./cmd/nano-brain/ -run TestRunInteractiveInit` green
- `go test -race -short ./cmd/nano-brain/...` green (no regression in sibling init/step tests)
- Credential-echo grep gate: zero raw `database.url`/password prints in init.go
- Manual UAT (per RESEARCH line 515, since ≤6-question count and summary formatting are not fully mechanically assertable): one end-to-end wizard run on a fresh config with Docker available, confirming ≤6 prompts to a running+registered+MCP-configured state
</verification>

<success_criteria>
`nano-brain init` (no args, TTY) walks a fresh machine from binary-installed to MCP-tools-working in ≤6 core questions: it gates on existing config (keep = resume), detects/provisions the DB, optionally enables embeddings, gates advanced settings, writes a 0600 config, runs doctor, starts the server, registers the workspace, configures the chosen MCP clients, and prints a summary ending in "restart your AI client" — composing the Wave-1/2 steps without re-implementing them and never echoing credentials.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 07)

Edited `cmd/nano-brain/init.go` (package main):
- `runInteractiveInit(configPath string)` restructured into the orchestrator sequence: TTY gate (D-04) → config-exists [k]eep/[o]verwrite gate (D-03) → `stepDatabaseFn` (Plan 03) → `stepEmbeddingFn` (Plan 04) → advanced gate (D-02, preserving the existing detailed prompts) → YAML assemble + 0600 write → `runDoctorChecksFn` (doctor.RunAll, print table) → `stepServeFn` (Plan 06) → register prompt + `registerWorkspaceFn` (Plan 05) → `promptMCPClientConfig` (D-16, once) → summary block (D-17)
- New orchestrator seams (default to the real functions): `stepDatabaseFn`, `stepEmbeddingFn`, `stepServeFn`, `registerWorkspaceFn`, `promptMCPClientConfigFn`, `runDoctorChecksFn`
- `defaultRunDoctorChecks(configPath string) []doctor.Check` — loads config + runs doctor.RunAll + prints the check table (without runDoctorCmd's os.Exit)
- `promptWithDefault` unchanged

New file `cmd/nano-brain/init_test.go`: `TestRunInteractiveInit_NonTTY`, `TestRunInteractiveInit_KeepExisting`, `TestRunInteractiveInit_QuestionBudget`, `TestRunInteractiveInit_AdvancedGate`, `TestRunInteractiveInit_Summary` (+ seam hook helper), all via injected seams — no real Docker/DB/daemon/network.
</artifacts_produced>

<output>
Create `.planning/phases/13-interactive-init-wizard-one-command-interactive-setup-detect/13-07-SUMMARY.md` when done.
</output>