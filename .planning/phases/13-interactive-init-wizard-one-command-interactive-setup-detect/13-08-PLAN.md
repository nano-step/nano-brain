---
phase: 13-interactive-init-wizard-one-command-interactive-setup-detect
plan: 08
type: execute
wave: 4
depends_on: ["13-07"]
files_modified:
  - docs/SETUP_AGENT.md
  - README.md
autonomous: true
requirements: [D-18]
must_haves:
  truths:
    - "docs/SETUP_AGENT.md leads with the one-command flow — prerequisites check (Node/Docker/optionally Ollama) → `npm install -g @nano-step/nano-brain` → `nano-brain init` → verify — describing the Phase 13 wizard from 13-07 (D-18)"
    - "The docs state that `nano-brain init` (no args, interactive) handles DB provision, embeddings, config, server start, workspace registration, and MCP client config in a single flow — the user is NOT told to run the old 10 manual steps for the happy path (D-18)"
    - "The current per-step manual instructions (Step 1..Step 10, VPS/team path) survive as a `## Manual setup` / troubleshooting appendix — nothing deleted, still valid for the VPS/team and no-Docker path (D-18)"
    - "The docker command, MCP config snippets, doctor failure table, and `?workspace=` binding note remain reachable in the appendix — the appendix is the fallback the wizard's non-interactive / VPS callers still use (D-18)"
    - "README's Start section is `npm install -g @nano-step/nano-brain && nano-brain init` (the two-line one-command flow), replacing the old `docker run … && nano-brain serve -d && nano-brain init --root=…` sequence (D-18)"
    - "No Go source, no test, no config, and no go.mod is touched — this plan edits only docs/SETUP_AGENT.md and README.md (`go build ./cmd/nano-brain/` still succeeds, `git diff go.mod` empty)"
  artifacts:
    - "docs/SETUP_AGENT.md (rewritten: one-command flow at the top, `## Manual setup` appendix preserving the old Step 1..10 + VPS/team path)"
    - "README.md (Start section updated to `npm install -g @nano-step/nano-brain && nano-brain init`)"
  key_links:
    - "SETUP_AGENT.md one-command flow → `nano-brain init` wizard (13-07 runInteractiveInit orchestrator) — the doc describes the D-01..D-17 flow the wizard actually implements"
    - "SETUP_AGENT.md `## Manual setup` appendix ← the current Step 1..Step 10 + VPS/team content, moved verbatim-in-spirit (still valid for the no-Docker / VPS path per D-18 and CONTEXT specifics line 91 Windows note)"
    - "README.md Start section → SETUP_AGENT.md (README links to the full setup guide for detail)"
    - "Docker command in appendix == the wizard's provisioning command (D-06: `nanobrain-pg`, `pgvector/pgvector:pg17`, `restart unless-stopped`, nanobrain/nanobrain/nanobrain_dev) — docs and wizard mental model stay consistent"
---

<objective>
Rewrite `docs/SETUP_AGENT.md` around the new one-command flow delivered by Phase 13's `nano-brain init` wizard, and update README's Start section to match — implementing D-18 (the docs decision that had zero implementing task before this plan).

Purpose: Phase 13 (Plans 13-01..13-07) turns `nano-brain init` into a single interactive command that walks a user from "binary installed" to "MCP tools working" — detect/provision PostgreSQL → optional embeddings → write config → start server → register workspace → configure MCP client → summary. The setup docs still describe the superseded 10-step manual flow ("start the server and run `init --root` manually"), which is now the dead-end the wizard replaces. D-18 requires the docs lead with the one-command flow while preserving the manual steps as a troubleshooting / VPS-team appendix (they remain the correct path when there is no Docker, or for a shared remote server). This is the docs half of the phase; no code changes.
Output: `docs/SETUP_AGENT.md` restructured so the primary path is the four-beat one-command flow (prerequisites → `npm install -g @nano-step/nano-brain` → `nano-brain init` → verify), with the existing Step 1..Step 10 detail and VPS/team section relocated under a `## Manual setup` appendix; and `README.md` Start section reduced to `npm install -g @nano-step/nano-brain && nano-brain init`. No other files touched.
</objective>

<execution_context>
@/Users/tamlh/workspaces/self/AI/Tools/nano-brain/.claude/gsd-core/workflows/execute-plan.md
@/Users/tamlh/workspaces/self/AI/Tools/nano-brain/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-interactive-init-wizard-one-command-interactive-setup-detect/13-CONTEXT.md
@.planning/phases/13-interactive-init-wizard-one-command-interactive-setup-detect/13-07-PLAN.md
@docs/SETUP_AGENT.md
@README.md
@CLAUDE.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite docs/SETUP_AGENT.md — one-command flow on top, manual steps to appendix (D-18)</name>
  <files>docs/SETUP_AGENT.md</files>
  <read_first>
    - docs/SETUP_AGENT.md (the file being rewritten — the whole current 10-step flow: Step 1 Node, Step 2 Docker, Step 3 Ollama, Step 4 Docker postgres command lines 76-89, Step 5 npm install, Step 6 doctor + failure table, Step 7 serve, Step 8 register, Step 9 MCP config snippets + `?workspace=` binding, Step 10 verify, Troubleshooting, VPS/team Path 2 lines 266-283)
    - .planning/phases/13-interactive-init-wizard-one-command-interactive-setup-detect/13-CONTEXT.md (D-18 exact wording line 49; the six core questions D-01 line 24; database detect→Docker→remote D-05..D-09; embeddings optional D-11..D-12; chain-to-running D-14..D-17; the Windows serve caveat in specifics line 91; the "one command is the acceptance bar" specifics line 90)
    - .planning/phases/13-interactive-init-wizard-one-command-interactive-setup-detect/13-07-PLAN.md (the ACTUAL wizard flow the docs must describe: TTY gate → keep/overwrite → DB → embedding → advanced gate → write+doctor → serve → register → MCP → summary, and the D-17 summary ending in "restart your AI client")
  </read_first>
  <action>
    Rewrite docs/SETUP_AGENT.md so the PRIMARY, first-read path is the one-command flow, and the existing manual detail becomes a clearly-labelled appendix. Keep the file's audience framing (a guide AI agents can follow to walk a user through setup). Concretely:

    (A) Keep the top `# nano-brain Setup Guide for AI Agents` title and intro sentence.

    (B) Replace the current "Before you start" three-path chooser and the Step 1..Step 10 primary sequence with a new `## Quick setup (one command)` section that presents four beats, in this order, matching the 13-07 wizard:
      1. Prerequisites check — Node.js 18+ (required for the npm install), Docker (recommended: the wizard auto-provisions PostgreSQL via Docker; optional if the user will supply a remote PostgreSQL URL), and optionally Ollama (only if the user wants local embeddings — the wizard also accepts any Ollama-compatible URL or Voyage, and embeddings can be skipped for BM25-only). Give the same `node --version` / `docker info` / `ollama --version` check commands the current Steps 1-3 use, but framed as a lightweight prerequisites checklist rather than gated install steps.
      2. Install — `npm install -g @nano-step/nano-brain` (one code block).
      3. Initialize — `nano-brain init` (no args). Explain in prose that this single interactive command: gates on an existing config (keep = resume setup, safely re-runnable), detects a reachable PostgreSQL or offers to start one via Docker (or accepts a remote URL), optionally enables embeddings (local Ollama / any Ollama-compatible URL / Voyage / skip for BM25-only), writes the config, runs doctor, starts the server, registers the current directory as a workspace, and configures the MCP client(s) the user selects — then prints a summary whose final action is "restart your AI client". State that the happy path is roughly a handful of prompts (the core flow asks at most six consequential questions). Do NOT enumerate a re-derived question list that could drift from CONTEXT — describe the flow, and point to the appendix for per-step manual control.
      4. Verify — after restarting the AI client, the MCP tools respond (e.g. `memory_status` returns healthy); include the existing quick end-to-end check (write a memory, query it back) as the verify beat.

    (C) Add a one-line note under the Initialize beat that on Windows the daemon auto-start is not available yet, so the wizard will print a manual `nano-brain serve` instruction instead of starting the server itself (per CONTEXT specifics line 91) — direct Windows users to the manual appendix for the serve step.

    (D) Add a `## Manual setup / troubleshooting` appendix heading, and MOVE the entire current manual content under it, essentially unchanged: the Step 1..Step 10 detail (Node, Docker, Ollama, the Docker postgres `docker run … nanobrain-pg … pgvector/pgvector:pg17` command exactly as-is, npm install, doctor + the failure/fix table, serve, register via `init --root`, the MCP client config snippets for Claude Code / OpenCode / other clients, the `?workspace=` default-binding note, and the end-to-end verify), the Troubleshooting section, and the VPS / team setup (Path 2). Prefix the appendix with a short sentence: this manual, per-step path stays valid and is the recommended route for a VPS / shared-team server, a machine without Docker (bring your own PostgreSQL), Windows, or when you want step-by-step control instead of the wizard. Do NOT delete or shrink any of the manual instructions — they must survive intact so the VPS/team and no-Docker paths keep working (D-18 explicitly says do not delete them).

    (E) Make sure the doc still contains the literal string `## Manual setup` (the appendix heading) and that the top-of-file quick-setup section references `nano-brain init` and `npm install -g @nano-step/nano-brain`.

    Use the Read tool to load the current file, then Edit/Write it. This is a documentation edit only — do not touch any Go file, test, or config.
  </action>
  <verify>
    <automated>cd /Users/tamlh/workspaces/self/AI/Tools/nano-brain && echo "--- one-command flow present ---" && grep -c 'npm install -g @nano-step/nano-brain' docs/SETUP_AGENT.md && grep -c 'nano-brain init' docs/SETUP_AGENT.md && echo "--- manual appendix preserved ---" && grep -c '## Manual setup' docs/SETUP_AGENT.md && echo "--- manual docker command intact (D-06 parity) ---" && grep -c 'pgvector/pgvector:pg17' docs/SETUP_AGENT.md && grep -c 'nanobrain-pg' docs/SETUP_AGENT.md && echo "--- VPS/team path preserved ---" && grep -ci 'VPS' docs/SETUP_AGENT.md && echo "--- no code touched ---" && go build ./cmd/nano-brain/ && git diff --name-only | grep -vE '^(docs/SETUP_AGENT.md|README.md)$' | grep -E '\.go$|go\.mod$' | wc -l | sed 's/^/non-doc code files changed (must be 0): /'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'npm install -g @nano-step/nano-brain' docs/SETUP_AGENT.md` ≥ 1 (one-command install present)
    - `grep -c 'nano-brain init' docs/SETUP_AGENT.md` ≥ 1 and it appears in the top quick-setup section (not only inside `init --root` in the appendix)
    - `grep -c '## Manual setup' docs/SETUP_AGENT.md` == 1 (appendix heading exists)
    - The Docker provisioning command survives in the appendix: `grep -c 'pgvector/pgvector:pg17'` ≥ 1 and `grep -c 'nanobrain-pg'` ≥ 1 (D-06 parity with the wizard)
    - The VPS / team path survives: `grep -ci 'VPS'` ≥ 1
    - The doctor failure/fix table, MCP client config snippets, and `?workspace=` binding note are still present in the appendix (spot-checked: `grep -c 'workspace='` ≥ 1, `grep -c 'mcpServers\|"type": "remote"'` ≥ 1)
    - The quick-setup section ends its init description with the "restart your AI client" next-action wording matching the D-17 summary
    - `go build ./cmd/nano-brain/` succeeds and no `.go`/`go.mod` file is in the diff (docs-only change)
  </acceptance_criteria>
  <done>docs/SETUP_AGENT.md leads with the four-beat one-command flow (prerequisites → npm install → `nano-brain init` → verify) describing the 13-07 wizard, and preserves the full Step 1..10 manual instructions + VPS/team path under a `## Manual setup / troubleshooting` appendix — nothing deleted, no code touched.</done>
</task>

<task type="auto">
  <name>Task 2: Update README Start section to the one-command flow (D-18)</name>
  <files>README.md</files>
  <read_first>
    - README.md (the `### Install` block lines 15-23 and the `### Start` block lines 25-38 to replace; the `## Quick Start` Prerequisites lines 226-233 and Configuration section lines 452-475 for consistency)
    - .planning/phases/13-interactive-init-wizard-one-command-interactive-setup-detect/13-CONTEXT.md (D-18 line 49: "Update README's Start section to `npm install -g @nano-step/nano-brain && nano-brain init`")
    - docs/SETUP_AGENT.md (as rewritten by Task 1 — the README Start section should point here for the full flow)
  </read_first>
  <action>
    Update README.md's `### Start` section (currently the three-command `docker run … nanobrain-pg …` → `nano-brain serve -d` → `nano-brain init --root=…` sequence, lines 27-38) so the primary start path is the one-command flow. Replace the code block with:

    ```
    npm install -g @nano-step/nano-brain && nano-brain init
    ```

    (or the same two commands on separate lines within one block), and add one prose line beneath it: `nano-brain init` is an interactive wizard that provisions PostgreSQL (via Docker or a remote URL), optionally enables embeddings, starts the server, registers your project, and configures your MCP client — then tells you to restart your AI client. Add a pointer sentence: "For a manual, per-step setup (VPS / team, no Docker, or Windows), see [docs/SETUP_AGENT.md](docs/SETUP_AGENT.md)."

    Keep the `### Install` block above it as-is (it already shows the npm install and build-from-source options) — but ensure the Start section no longer instructs the user to run `docker run` + `nano-brain serve -d` + `nano-brain init --root=…` as the primary path (that manual sequence now lives in SETUP_AGENT.md's appendix; do not duplicate it as the README's primary Start path). You MAY keep a single "Manual start" one-liner link to the appendix, but do not reproduce the full docker/serve/init-root sequence inline.

    Leave the `## Quick Start` → `### Configure Your AI Agent` MCP snippet and the `## Configuration` YAML example unchanged (they document the config/MCP shape, which the wizard now writes for the user — still accurate as reference). This is a documentation edit only — no Go/config/test changes.
  </action>
  <verify>
    <automated>cd /Users/tamlh/workspaces/self/AI/Tools/nano-brain && echo "--- README one-command start present ---" && grep -c 'npm install -g @nano-step/nano-brain && nano-brain init' README.md && echo "--- README points to setup guide ---" && grep -c 'docs/SETUP_AGENT.md' README.md && echo "--- old manual docker+serve+init-root no longer the primary Start block ---" && awk '/^### Start/{f=1} f&&/^## /{f=0} f' README.md | grep -c 'serve -d' | sed 's/^/serve -d in Start section (should be 0): /' && echo "--- no code touched ---" && go build ./cmd/nano-brain/ && git diff --name-only | grep -E '\.go$|go\.mod$' | wc -l | sed 's/^/code files changed (must be 0): /'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'npm install -g @nano-step/nano-brain && nano-brain init' README.md` == 1 (the exact D-18 one-command Start string)
    - The README `### Start` section no longer contains the primary `docker run … nanobrain-pg` + `nano-brain serve -d` + `nano-brain init --root=` three-step sequence as its main path (the `serve -d` count inside the Start section is 0)
    - README's Start section links to `docs/SETUP_AGENT.md` for the manual / VPS path (`grep -c 'docs/SETUP_AGENT.md' README.md` ≥ 1)
    - `### Install` block and the `## Configuration` YAML example are unchanged
    - `go build ./cmd/nano-brain/` succeeds; no `.go`/`go.mod` file changed
  </acceptance_criteria>
  <done>README's Start section is the one-command `npm install -g @nano-step/nano-brain && nano-brain init` flow with a prose description and a link to SETUP_AGENT.md for the manual/VPS path; the old three-step docker/serve/init-root sequence is no longer the primary Start block; no code touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → operator following setup | The only "input" is human/agent reading the guide; stale or misleading instructions are the entire threat surface for a docs-only change |
| docs → downstream tooling (no runtime) | Docs are not executed; no code path, no credential, no network is introduced by this plan |

## STRIDE Threat Register (ASVS L1 — docs-only plan)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-15 | Information Disclosure / Tampering | SETUP_AGENT.md manual appendix | low | mitigate | The manual steps (Docker command, MCP config, VPS/team, `?workspace=` binding) are preserved verbatim-in-spirit so no-Docker / VPS / Windows users are not stranded by an incomplete quick-setup section; grep gates assert the appendix content survives |
| T-13-16 | Tampering | README / SETUP_AGENT drift from the shipped wizard | low | mitigate | The docs describe the 13-07 wizard flow and reuse the same D-06 Docker command string, keeping docs and the actual `nano-brain init` behavior consistent (specifics line 89); no re-derived question list that could silently diverge from CONTEXT |
| T-13-SC | Tampering | npm/pip/cargo installs | low | accept | N/A — docs-only plan; zero new packages, no install task, no code executed (RESEARCH Package Legitimacy Audit: this phase adds no dependency). `go build` sanity in each task proves no code was touched |
</threat_model>

<verification>
- `grep -c 'npm install -g @nano-step/nano-brain' docs/SETUP_AGENT.md` ≥ 1 and `grep -c 'nano-brain init' docs/SETUP_AGENT.md` ≥ 1 (one-command flow leads the doc)
- `grep -c '## Manual setup' docs/SETUP_AGENT.md` == 1 (manual appendix preserved)
- `grep -c 'pgvector/pgvector:pg17' docs/SETUP_AGENT.md` ≥ 1 and `grep -c 'nanobrain-pg' docs/SETUP_AGENT.md` ≥ 1 (D-06 Docker command parity survives in the appendix)
- `grep -ci 'VPS' docs/SETUP_AGENT.md` ≥ 1 (VPS/team path preserved)
- `grep -c 'npm install -g @nano-step/nano-brain && nano-brain init' README.md` == 1 (D-18 Start string)
- `grep -c 'docs/SETUP_AGENT.md' README.md` ≥ 1 (README links to the full guide)
- `go build ./cmd/nano-brain/` succeeds and `git diff --name-only | grep -E '\.go$|go\.mod$'` is empty (docs-only; no code touched — the required build sanity per revision spec)
- No test suite needed: this plan produces documentation only; correctness is verified by grep assertions above plus the build sanity proving zero code change.
</verification>

<success_criteria>
`docs/SETUP_AGENT.md` and `README.md` both present the Phase 13 one-command flow — `npm install -g @nano-step/nano-brain && nano-brain init` — as the primary setup path, describing the interactive wizard from 13-07 (DB provision → embeddings → serve → register → MCP config → "restart your AI client"), while the full existing per-step manual instructions and VPS/team path survive intact under a `## Manual setup / troubleshooting` appendix. No Go source, test, or config is modified, and `go build ./cmd/nano-brain/` still succeeds — closing D-18, the docs decision that previously had no implementing task.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 08)

Edited `docs/SETUP_AGENT.md`:
- New `## Quick setup (one command)` section at the top: four beats (prerequisites check Node/Docker/optionally-Ollama → `npm install -g @nano-step/nano-brain` → `nano-brain init` → verify), describing the 13-07 wizard (keep/overwrite resume, DB detect/provision/remote, optional embeddings, serve, register, MCP config, "restart your AI client" summary), plus a Windows-serve caveat pointing to the appendix.
- New `## Manual setup / troubleshooting` appendix preserving the entire prior Step 1..Step 10 detail, the doctor failure/fix table, the MCP client config snippets, the `?workspace=` binding note, the Troubleshooting section, and the VPS / team (Path 2) setup — intact, still valid for VPS/team, no-Docker, and Windows paths (D-18).

Edited `README.md`:
- `### Start` section replaced with the one-command `npm install -g @nano-step/nano-brain && nano-brain init` flow + a prose description of the wizard + a link to `docs/SETUP_AGENT.md` for the manual/VPS path. The old `docker run` + `serve -d` + `init --root` three-step sequence is no longer the primary Start block.
- `### Install`, `## Quick Start` MCP snippet, and `## Configuration` YAML example left unchanged (still accurate reference for what the wizard writes).
</artifacts_produced>

<output>
Create `.planning/phases/13-interactive-init-wizard-one-command-interactive-setup-detect/13-08-SUMMARY.md` when done.
</output>
