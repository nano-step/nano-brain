---
phase: 999.1-reindex-full-on-worktree-checkout-perf
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/00029_add_documents_mod_time_file_size.sql
  - internal/storage/queries/documents.sql
  - internal/storage/sqlc/documents.sql.go
  - internal/storage/sqlc/models.go
autonomous: true
requirements: []
must_haves:
  truths:
    - "The documents table has nullable mod_time TIMESTAMPTZ and file_size BIGINT columns after migration."
    - "The migration applies cleanly on an existing populated documents table without rewriting or losing rows."
    - "UpsertDocumentBySourcePath accepts and persists mod_time + file_size."
    - "A bulk query returns (source_path, mod_time, file_size, content_hash) per workspace+collection for cache preloading."
  artifacts:
    - "migrations/00029_add_documents_mod_time_file_size.sql — goose Up/Down adding mod_time + file_size to documents."
    - "internal/storage/queries/documents.sql — UpsertDocumentBySourcePath extended; new PreloadFileStateByWorkspace query."
    - "Regenerated internal/storage/sqlc/documents.sql.go + models.go reflecting the new columns and query."
  key_links:
    - "sqlc generate consumes migrations/ as schema and queries/ as source then regenerates Go in internal/storage/sqlc/."
    - "PreloadFileStateByWorkspace feeds the bulk cache warm in plan 03."
  prohibitions:
    - "MUST NOT make mod_time or file_size NOT NULL (existing rows have neither; unchanged rows get them lazily on next index, per CONTEXT caveat)."
    - "MUST NOT alter or drop any existing documents column, index, or constraint."
    - "MUST NOT hand-edit the generated sqlc files except via sqlc generate."
---

<objective>
Implement the schema + query layer for Fix B (D-06b): add nullable `mod_time TIMESTAMPTZ` and `file_size BIGINT` columns to `documents`, extend `UpsertDocumentBySourcePath` to persist them, and add a bulk `PreloadFileStateByWorkspace` query returning `(source_path, mod_time, file_size, content_hash)` for warming the watcher in-memory cache at startup.

Purpose: Give the in-memory mtime+size fast-path a durable backing store so it survives process restart and new-worktree registration. This plan delivers only the persistence layer; plan 03 wires it into the watcher.

Output: a new goose migration, extended + new sqlc queries, and the regenerated Go.
</objective>

<execution_context>
@/Users/tamlh/workspaces/self/AI/Tools/nano-brain/.claude/gsd-core/workflows/execute-plan.md
@/Users/tamlh/workspaces/self/AI/Tools/nano-brain/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.1-reindex-full-on-worktree-checkout-perf/999.1-CONTEXT.md
</context>

<artifacts_produced>
## Artifacts this plan produces
- New migration `migrations/00029_add_documents_mod_time_file_size.sql` (goose Up/Down).
- New columns `documents.mod_time` (TIMESTAMPTZ NULL) and `documents.file_size` (BIGINT NULL).
- Extended sqlc query `UpsertDocumentBySourcePath` (two new params: mod_time, file_size).
- New sqlc query `PreloadFileStateByWorkspace` returning `(source_path, mod_time, file_size, content_hash)`.
- Regenerated `internal/storage/sqlc/documents.sql.go` and `internal/storage/sqlc/models.go`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add migration for nullable mod_time + file_size on documents</name>
  <files>migrations/00029_add_documents_mod_time_file_size.sql</files>
  <read_first>
    - migrations/00001_initial_schema.sql — `documents` table (~14-27); confirms column set and the `uq_documents_content_hash_workspace` constraint to leave intact.
    - migrations/00028_add_documents_tags_gin_index.sql — latest migration; confirms next number is 00029 and the goose header style.
    - migrations/00015_add_documents_timestamp_indexes.sql — example migration with `-- +goose Up` / `-- +goose Down`.
    - migrations/migrations.go — `//go:embed *.sql`; a new numbered `.sql` is auto-included, no registration needed.
  </read_first>
  <action>
    Create `migrations/00029_add_documents_mod_time_file_size.sql` with goose `-- +goose Up` / `-- +goose Down` sections. Up adds two nullable columns with no default: an `ADD COLUMN IF NOT EXISTS mod_time TIMESTAMPTZ` and an `ADD COLUMN IF NOT EXISTS file_size BIGINT` (metadata-only, safe on a large populated table, no row rewrite). Down drops both with `DROP COLUMN IF EXISTS file_size` then `DROP COLUMN IF EXISTS mod_time`. Use a plain transactional migration (do NOT add the no-transaction goose marker; that is only needed for concurrent index builds, not nullable column adds). Add a short header comment referencing phase 999.1 / D-06b and the rationale (persist the watcher fast-path fingerprint). Do NOT touch any existing column, index, or constraint.
  </action>
  <verify>
    <automated>cd /Users/tamlh/workspaces/self/AI/Tools/nano-brain && CGO_ENABLED=0 go build ./migrations/... && grep -c "ADD COLUMN IF NOT EXISTS" migrations/00029_add_documents_mod_time_file_size.sql</automated>
  </verify>
  <done>Migration file exists with two ADD COLUMN statements (grep count 2) and matching DROP COLUMN statements in the Down section; the migrations package builds.</done>
</task>

<task type="auto">
  <name>Task 2: Extend UpsertDocumentBySourcePath, add PreloadFileStateByWorkspace, then sqlc generate</name>
  <files>internal/storage/queries/documents.sql, internal/storage/sqlc/documents.sql.go, internal/storage/sqlc/models.go</files>
  <read_first>
    - internal/storage/queries/documents.sql — `UpsertDocumentBySourcePath :one` (~15-27): current 9 params ($1-$9) and the `ON CONFLICT ... DO UPDATE SET` list; sibling `UpsertDocument :one` (~1-13) which must stay unchanged; `ListDocumentSourcePathsAndHashes :many` (~125-131) as the model for a per-workspace+collection projection.
    - sqlc.yaml — engine postgresql; queries `internal/storage/queries/`; schema `migrations/`; out `internal/storage/sqlc/`; package `sqlc`.
    - internal/storage/sqlc/documents.sql.go — `UpsertDocumentBySourcePathParams` (~682) and `UpsertDocumentBySourcePathRow` (the shape that will change after regen).
    - internal/storage/sqlc/models.go — `Document` struct (~82) which will gain `ModTime` + `FileSize` after regen.
  </read_first>
  <action>
    Edit `internal/storage/queries/documents.sql`:
    (1) Extend `UpsertDocumentBySourcePath`: add `mod_time` and `file_size` to the INSERT column list and as `$10`, `$11` in VALUES; in `DO UPDATE SET` add `mod_time = EXCLUDED.mod_time` and `file_size = EXCLUDED.file_size`. Keep `RETURNING id, content_hash, collection, workspace_hash` unchanged. The two new params must accept NULL — they should generate as `sql.NullTime` and `sql.NullInt64`. Leave the sibling `UpsertDocument` query untouched.
    (2) Add `-- name: PreloadFileStateByWorkspace :many` selecting `source_path, mod_time, file_size, content_hash FROM documents WHERE workspace_hash = $1 AND collection = $2 AND source_path != '' AND mod_time IS NOT NULL AND file_size IS NOT NULL`. Filtering out NULL fingerprints keeps the warmed cache correct: rows indexed before this migration have no fingerprint and must fall through to normal processing (CONTEXT lazy-backfill caveat).
    Then run `sqlc generate`. Confirm `UpsertDocumentBySourcePathParams` gained `ModTime` + `FileSize`; the `Document` model gained `ModTime sql.NullTime` + `FileSize sql.NullInt64`; and a new `PreloadFileStateByWorkspace` method plus `PreloadFileStateByWorkspaceParams` and `PreloadFileStateByWorkspaceRow` exist. Do NOT hand-edit the generated files. Per D-06b.
  </action>
  <verify>
    <automated>cd /Users/tamlh/workspaces/self/AI/Tools/nano-brain && sqlc generate && CGO_ENABLED=0 go build ./internal/storage/... && grep -q "func (q \*Queries) PreloadFileStateByWorkspace" internal/storage/sqlc/documents.sql.go && grep -Eq "ModTime[[:space:]]+sql.NullTime" internal/storage/sqlc/models.go && grep -Eq "FileSize[[:space:]]+sql.NullInt64" internal/storage/sqlc/models.go && echo OK</automated>
  </verify>
  <done>`sqlc generate` succeeds and `internal/storage/...` builds. The new method, both new param fields, and the two new Document model fields exist; the prints confirm OK.</done>
</task>

</tasks>

<threat_model>
Internal persistence-layer change. The new columns are nullable, write-path-only fingerprint metadata (mtime, byte size) already derivable from the filesystem — no secrets, no new external input, no new endpoints. The migration is additive and reversible. The single new read query is scoped by `workspace_hash` + `collection` (the existing tenancy boundary), so it introduces no cross-workspace data exposure. No STRIDE register required for this plan.
</threat_model>

<verification>
- `sqlc generate` runs clean and `CGO_ENABLED=0 go build ./internal/storage/...` passes.
- Migration package builds; the new file has 2 ADD COLUMN and 2 DROP COLUMN statements.
- Generated code exposes `PreloadFileStateByWorkspace`, the extended `UpsertDocumentBySourcePathParams`, and the `Document` model fields `ModTime sql.NullTime` + `FileSize sql.NullInt64`.
</verification>

<success_criteria>
- documents has nullable mod_time + file_size after migration; existing rows untouched.
- UpsertDocumentBySourcePath persists both new columns; PreloadFileStateByWorkspace returns only fully-fingerprinted rows.
- Regenerated sqlc compiles with no hand edits.
</success_criteria>

<output>
Create `.planning/phases/999.1-reindex-full-on-worktree-checkout-perf/999.1-02-SUMMARY.md` when done.
</output>