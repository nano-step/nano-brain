# Phase 999.1: Avoid full reindex on git worktree create / checkout - Context

**Gathered:** 2026-06-29
**Status:** Ready for planning (BACKLOG — not for immediate fix)

<domain>
## Phase Boundary

Reduce indexing lag on three triggers that share one root cause:
1. **`serve` startup** — machine lags for a while right after starting nano-brain (user-reported: "thấy index hoặc chunks rất nhiều").
2. **New git worktree** — creating a worktree triggers the same lag.
3. **Git checkout / branch switch** — rewrites file mtimes, defeating the cheap skip.

This phase clarifies WHAT to fix and HOW we'd verify it. It does NOT implement the fix yet.

**Out of scope:** changing the embedding pipeline, search/ranking, or chunking logic. This is strictly about *deciding what to re-examine* on these triggers.
</domain>

<decisions>
## Implementation Decisions

### Reframing — the original assumption was partly wrong (D-01)
- **D-01:** Content-hash dedup ALREADY EXISTS at two levels — document (`internal/watcher/watcher.go:757`) and chunk (`internal/db/chunks.sql.go:174-177`). A checkout/worktree with identical content does **NOT** re-embed. So the lag is **not** redundant embedding — it is the full filesystem scan + per-file SHA256 + per-file DB lookup that runs because the cheap skip is defeated.

### Root cause (D-02)
- **D-02:** The cheap skip — mtime+size cache (`fileState`, `watcher.go:62-66`, checked at `watcher.go:722`) — is **in-memory only**. It is empty on every process start and for every new worktree, so every file falls through to Stage-2 hashing. Git checkout additionally rewrites mtimes, defeating the cache even within a long-running process. This one fact explains all three symptoms.

### Scope (D-03)
- **D-03:** Address all three triggers together — they share the root cause (non-persistent cheap-skip fingerprint + zero git-awareness in the watcher). Startup-serve lag is the most frequent and was the user's strongest signal.

### Direction to research (D-04)
- **D-04:** Primary direction: **persist a cheap file fingerprint to the DB** so the skip survives restart and worktree creation (a plain restart with unchanged mtimes should skip hashing entirely). mtime alone won't help the checkout case (git rewrites it) — so research what a robust fingerprint is (size + mtime + possibly inode, or a stored content hash compared cheaply).
- **D-04b:** Research how established tools solve exactly this and follow the proven pattern rather than inventing one. Candidates to study: Watchman, ctags/gtags, Sourcegraph/zoekt, ripgrep, LSP/tree-sitter indexers, git's own index/`stat` cache. The user explicitly wants "research các tool khác đang làm thế nào và làm theo."
- **D-04c:** Consider git-awareness for the checkout case specifically: detect HEAD change and use `git diff --name-only old..new` to examine only changed files instead of a full scan. Keep as a researched option, not yet locked.

### Verify-first gate (D-05) — HARD GATE
- **D-05:** Researcher MUST measure the real lag on a large repo BEFORE any fix is planned: scan duration, number of files hashed, number of DB lookups, and whether any embedding is actually triggered. The plan is gated on these numbers — no fix lands without a measured baseline. This is because the original assumption ("everything re-embeds") was already shown to be partly false; measure, don't guess.

### Claude's Discretion
- Exact fingerprint scheme and storage column/migration shape — for research + planning to determine from the tool survey.
- Whether git-awareness (D-04c) ships alongside the persisted fingerprint or as a follow-up — depends on measured contribution of the checkout case vs the startup case.
</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Indexing trigger path (the code under investigation)
- `internal/watcher/watcher.go` — fsnotify setup (`:20`), event filter Create|Write|Remove|Rename (`:367`), debounce (`:317-318`, `:409`), `handleFSEvent` (`:366`), in-memory `fileState` cache (`:62-66`), mtime+size skip (`:722`), SHA256 + `GetDocumentBySourcePath` doc-hash skip (`:746-757`), `processAll`/`processDirty` startup path (`:328`).
- `internal/db/chunks.sql.go:174-177` — chunk-level `embed_status` preservation on unchanged content (the dedup that prevents re-embedding).
- `internal/server/handlers/reindex.go` — incremental reindex (`:137-274`, walks disk, compares `content_hash`) and force-wipe (`:89-135`, resets all chunks to `pending`).
- `cmd/nano-brain/main.go:545` — startup wiring that calls into the watcher's `processAll`.

### Project constraints
- `.planning/PROJECT.md` — "Performance: sub-50ms latency for code intelligence tools"; single static binary; no breaking changes to MCP tools.

*(Exact line numbers are from the 2026-06-29 scout and may drift — re-grep before relying on them.)*
</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- Content hashing + `GetDocumentBySourcePath` already exist — a persisted fingerprint can reuse the same hash, just compared earlier/cheaper.
- The two-stage skip architecture is already in place; the fix is making Stage-1 durable, not adding a new stage.

### Established Patterns
- Stage-1 (cheap mtime+size) → Stage-2 (content hash) → embed. Preserve this ordering; the change is persisting Stage-1 state.
- Incremental reindex handler already walks disk and compares `content_hash` — a model for the "examine only what changed" logic.

### Integration Points
- DB schema (sqlc — `sqlc generate` after SQL changes) for the persisted fingerprint column.
- Watcher startup (`processAll`) and worktree/workspace resolution are where a persisted fingerprint would be loaded.
</code_context>

<specifics>
## Specific Ideas

- User signal verbatim: "khi tôi start nano-brain serve lên là thấy index hoặc chunks rất nhiều ... khi serve thì máy lag một thời gian. khi tạo worktree thì cũng vậy."
- User wants the solution to mirror how mature indexing tools handle restart + git operations, not a bespoke design.
</specifics>

<deferred>
## Deferred Ideas

- **The actual fix implementation** — explicitly deferred. This is a backlog investigation; user said "chưa cần fix ngay bây giờ."
- **Parallelizing/batching the scan** (D-04 alternative "tăng tốc scan") — kept as a fallback if persisted fingerprint + git-awareness prove insufficient after measurement. Not the primary direction.

None of the above expand scope — they are sequencing notes within this phase's domain.
</deferred>

---

*Phase: 999.1-reindex-full-on-worktree-checkout-perf*
*Context gathered: 2026-06-29*
