# Story 9.3 — Pre-work gate evidence

**Date**: 2026-05-30
**Branch**: `feat/9.3-link-extractor` off `b-main` @ `1a4826a`
**Issue**: nano-step/nano-brain#243 (parent Epic #239)
**Worktree**: `.opencode/worktrees/story-9.3-link-extractor/story-9.3-link-extractor/`

## `./scripts/harness-check.sh pre-work --issue 243`

```
─ PRE-WORK checks
[FAIL] 1.1 Open PRs still pending (1+)
[PASS] 1.2 No active OpenSpec changes
[PASS] 1.3 Issue #243 exists (state: OPEN)
[PASS] 1.4 b-main is up-to-date
[PASS] 1.5 Validation ladder passes
[PASS] 1.6 Branch 'feat/9.3-link-extractor' is based on b-main

Summary: 5 PASS, 1 FAIL, 0 SKIP (total: 6)
```

## [HARNESS-OVERRIDE] Gate 1.1

Story 9.3 is **deliberately running in parallel** with Story 9.1 (PR #241) per Epic 9 execution plan
(`.sisyphus/plans/epic-9-execution-plan.md`). 9.3 has ZERO file overlap with 9.1:

- 9.1 touches `internal/eventbus/` only
- 9.3 touches: `migrations/00011_*.sql`, `internal/links/*.go`, `internal/storage/sqlc/edges.sql.go` (sqlc-generated),
  `internal/server/handlers/write.go` (add 2 lines), `internal/server/handlers/reindex.go` (add 2 lines),
  `internal/harvest/harvest.go` (add 2 lines)

The `internal/server/handlers/write.go` and `harvest/harvest.go` files MIGHT have eventbus.Publisher
injection added in 9.2 (next wave after 9.1 merges). 9.3 uses an interface-typed dependency — when 9.2
merges, the wiring will be confirmed; if 9.3 lands first, the extractor accepts a nil Publisher (no-op).

The other open PRs (#230 security hardening, #228 CI regression test) are pre-existing bot PRs
unrelated to Epic 9. See `docs/evidence/9.1-pre-work-gate.md` for the original Wave-A rationale.

**Override rationale**: Gate 1.1 enforces "previous PR merged" to keep the feature pipeline serial.
Epic 9 has approved a parallel-track strategy for 9.1 ∥ 9.3 (zero file overlap). Blocking 9.3 on 9.1's
merge would violate the execution plan and add ~1 day of wall-clock delay for no quality benefit.

**Compensating control**: 9.3 PR description will explicitly reference PR #241 and call out the
parallel-track strategy; reviewer can verify zero overlap at PR time.

All other gates (1.2–1.6) PASS without override.

**Verdict**: PROCEED with Story 9.3 implementation under documented gate-1.1 parallel-track override.
