Reviewer: gsd-code-reviewer (Sonnet)
Review Verdict: PASS

Findings:
- internal/flow/builder_test.go, WARNING: `TestFanoutCapKeepsIntegrationEdges` only exercises the main-loop fanout+integration-exemption fix (builder.go direct-calls branch, ~line 466-472); it does not exercise the symmetric fix inside the `EdgeReconcile` inner loop (builder.go ~line 411-417), which caps fanout and exempts integration/reconcile edges reached via a reconcile hop. Fix: add an analogous case with a reconcile edge whose target has >maxFanout calls edges followed by an integration edge, asserting the integration node still appears.

Rationale: Read builder.go end-to-end and traced both fanout-cap sites — integration (`EdgeIntegration`) and reconcile (`EdgeReconcile`) edges are classified and `continue`d in their own branches before either fanout check is reached, so they never increment or consume the `fanout` counter; `continue` (vs the old `break`) only skips the individual over-cap `calls` edge and lets the out-edge scan continue, so later integration/reconcile edges in the slice are still emitted. `fanout` is scoped per source node (`fanout := 0` at the top of each node's out-edge scan) and only ever increments, so the cap stays enforced for all subsequent calls edges — no over-emission, no double-counting, no unbounded loop (scan is bounded by the finite `outEdges`/`idx.bySource[...]` slices). Confirmed via `git stash` that `TestFanoutCapKeepsIntegrationEdges` fails on the pre-fix `break` ("integration edge after the fanout cap was dropped (#585 regression)") and passes on the `continue` fix. Grepped for other `break` in the function — one unrelated `break` remains at builder.go:338, inside the reconcile-ID membership scan (breaks the inner per-id loop after the first match), not part of the fanout logic, correctly left untouched. Both intended sites (builder.go pre-fix lines ~412 and ~467) were fixed identically. Ran `go vet ./internal/flow/...` and `go test -race -short ./internal/flow/...` — all tests pass, including pre-existing `TestFanoutCap`, `TestReconcileEdgeTraversal`, `TestNamespacedControllerReconciliation`, confirming no regression. The one gap found is a test-coverage gap, not a behavioral bug, so it does not block.
