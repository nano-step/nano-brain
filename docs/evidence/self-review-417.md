# Self-Review: Issue #417 — preserve embed_status on unchanged chunk upsert

## Change Summary

**What changed:** Modified `UpsertChunk` SQL query to conditionally reset `embed_status` based on content comparison.

**Files modified:**
- `internal/storage/queries/chunks.sql` — Fixed the UPSERT query
- `internal/storage/sqlc/chunks.sql.go` — Regenerated by sqlc
- `internal/storage/chunks_integration_test.go` — New integration tests

**Change type:** bug-fix
**Lane:** tiny

## Verification

### Build
- [x] `go build ./...` passes

### Unit Tests
- [x] `go test -race -short ./...` passes

### Integration Tests
- [x] `go test -race -tags=integration -run TestUpsertChunkPreservesEmbedStatus ./internal/storage/...` passes
- ⚠️ Pre-existing failures (not caused by this change):
  - `TestCrossWorkspacePermutations` in `internal/search` - search isolation test
  - `TestTimeFilter_REST_*` in `internal/server/handlers` - timefilter REST API tests
  - These failures exist on master and are unrelated to embed_status preservation

### Code Review
- [x] SQL query correctly uses CASE expression
- [x] sqlc regeneration successful
- [x] Integration tests cover all 4 scenarios from spec

## Evidence

### SQL Change
```sql
-- Before
embed_status = 'pending'

-- After
embed_status = CASE 
    WHEN EXCLUDED.content != chunks.content THEN 'pending'
    ELSE chunks.embed_status
END
```

### Integration Test Results
```
=== RUN   TestUpsertChunkPreservesEmbedStatus
=== RUN   TestUpsertChunkPreservesEmbedStatus/new_chunk_gets_pending_status
=== RUN   TestUpsertChunkPreservesEmbedStatus/unchanged_chunk_preserves_embed_status
=== RUN   TestUpsertChunkPreservesEmbedStatus/changed_chunk_resets_embed_status
=== RUN   TestUpsertChunkPreservesEmbedStatus/failed_chunk_with_unchanged_content_preserves_status
--- PASS: TestUpsertChunkPreservesEmbedStatus (0.17s)
```

## Conclusion

All verification steps pass. The change is minimal, focused, and correctly addresses the issue.
