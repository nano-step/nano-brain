# Self-Review: flow-dashboard (Phase 2 - UI Layer)

**Change Type:** user-feature
**Story:** flow-dashboard — Execution flow visualization dashboard
**Lane:** normal (2 risk flags: public-contracts, multi-domain)
**Date:** 2026-06-14
**Reviewer (Self):** Sisyphus (implementing — independent review delegated separately per R: no self-review)

## Summary

Phase 2 adds a UI dashboard for viewing execution flow diagrams. The dashboard:
1. **List API with pagination** — Cursor-based pagination for documents endpoint
2. **HTML template** — Standalone dashboard with Mermaid.js rendering
3. **UI route** — Serves dashboard at `/ui/flows`
4. **Client-side JS** — Workspace selector, endpoint list, Mermaid diagram rendering
5. **Styling** — Minimal v1 layout with sidebar and main content area

## Acceptance Criteria

- [x] **AC1** — `GET /api/v1/documents?collection=flows&workspace=<hash>&cursor=<ts,id>&limit=50` returns paginated results with `next_cursor`
- [x] **AC2** — `GET /ui/flows` serves HTML page with workspace selector, endpoint list, Mermaid rendering
- [x] **AC3** — CSP header allows `https://cdn.jsdelivr.net` for Mermaid.js
- [x] **AC4** — Build passes: `CGO_ENABLED=0 go build ./...`
- [x] **AC5** — Tests pass: `go test -race -short ./...`

## Diff scope

Files touched:
1. `internal/storage/queries/documents.sql` — New `ListDocumentsByWorkspacePaginated` query
2. `internal/storage/sqlc/documents.sql.go` — Regenerated by sqlc
3. `internal/server/handlers/documents.go` — Cursor/limit pagination support
4. `internal/server/handlers/documents_test.go` — Mock updated for new interface method
5. `internal/server/middleware/security_headers.go` — CSP allows Mermaid CDN
6. `internal/server/middleware/security_headers_test.go` — Test updated
7. `internal/server/webui/flow_dashboard.html` — **NEW** - Standalone dashboard
8. `internal/server/webui/embed.go` — Embeds flow_dashboard.html
9. `internal/server/webui/handler.go` — `RegisterFlowDashboardRoute`
10. `internal/server/routes.go` — Registers `/ui/flows` route

## Risk audit per FEATURE_INTAKE.md

| Flag | Applied? | Mitigation |
|---|---|---|
| Public contracts | **Yes** | Additive-only: new pagination params, new HTML endpoint. Default behavior unchanged. |
| Multi-domain | **Yes** | SQL query, HTTP handlers, HTML template, client-side JS. Same feature applied uniformly. |

**Total: 2 risk flags → Normal lane confirmed.**

## Validation

| Step | Result |
|---|---|
| `CGO_ENABLED=0 go build ./...` | clean |
| `go test -race -short ./...` | all packages pass |
| `go vet ./...` | clean |
| CSP includes `https://cdn.jsdelivr.net` | verified |
| `/ui/flows` route registered | verified |

## Scope discipline

Every changed line traces directly to flow-dashboard requirements:
- `ListDocumentsByWorkspacePaginated` → pagination API
- `flow_dashboard.html` → UI template
- `RegisterFlowDashboardRoute` → UI route
- CSP update → Mermaid CDN support

No adjacent refactoring. No formatting cleanups. No "improvements" outside scope.

## Known issues (pre-existing)

1. **golangci-lint warnings** — `newHTTPClient`, `httpVerbs`, `cleanRoutePath` are unused in pre-existing code (not introduced by this change)
2. **Integration tests timeout** — Require running PostgreSQL instance (expected behavior in CI)
