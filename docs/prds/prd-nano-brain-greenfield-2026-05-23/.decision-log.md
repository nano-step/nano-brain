# Decision Log — nano-brain v2 PRD

Append-only audit trail. Every decision, override, and change during PRD creation.

---

## 2026-05-23 — PRD v0.1.0-draft Created

- **Author**: Sisyphus (AI agent) via bmad-prd skill
- **Input**: Product brief, technical research (Go stack), reference PRD (v1)
- **Output**: 735-line PRD with 105 FRs, 7 UJs, 23 assumptions, 7 open questions

---

## 2026-05-23 — All 7 Open Questions Resolved (v0.2.0-draft)

User (BMad) reviewed PRD and resolved all 7 OQs:

| OQ | Decision | Rationale |
|---|---|---|
| OQ-1 | `scope=all` in all 9 MCP tools (not just HTTP) | "cần MCP, tôi nghĩ nó không quá nhiều công việc để làm thêm đâu" — thin wrapper, minimal effort |
| OQ-2 | `memory_write` captures `--supersedes` field in MVP; demotion logic Tier 2 | User agreed with recommendation: capture data now so Tier 2 has it |
| OQ-3 | PostgreSQL 17 (pinned `pgvector/pgvector:0.8.2-pg17`) | Librarian research confirmed: PG 17 GA stable 18+ months, pgvector 0.8.2 fully compatible, +25-40% JSON perf, 20x vacuum memory reduction, 2x WAL throughput. No reason to choose PG 16. |
| OQ-4 | Body (POST) + query string (GET); missing → HTTP 400 error; `"all"` → cross-workspace | "đơn giản thôi, đừng làm quá phức tạp, nếu không truyền thì raise lỗi, nếu truyền 'all' thì query tất cả" |
| OQ-5 | Adopt v1 baselines as-is (P@5=0.835, R@10=0.970, MRR=1.000) | User agreed with recommendation |
| OQ-6 | Server auto-migrate on startup + explicit `POST /api/reload-config` | User asked "nếu một process nào đó update config thì bạn sẽ làm gì?" → Option C (explicit reload endpoint) chosen. No fsnotify magic, no restart-only. Reloadable: collection globs, embedding settings, log level, search params. Non-reloadable: DB connection, listen port. |
| OQ-7 | Queue 10K in-memory, reject @50K (503 + Retry-After), status via GET /api/status, log warnings at 60%/90% | Librarian research: 100K chunks @ Ollama 3/s = 9.25 hrs drain. 10K queue = ~55 min backlog, ~1MB RAM. No SSE for MVP — polling via /api/status sufficient. |

### Research artifacts used for OQ-3 and OQ-7:
- **OQ-3**: Librarian agent `ses_1ad33efa6ffeMNc9QRMZKXHowf` — PG 16 vs 17 + pgvector compatibility
- **OQ-7**: Librarian agent `ses_1ad33a1dcffeu8LadXsKfbVbpB` — queue sizing, drain times, Go overflow patterns

### New FRs added:
- FR-72b: `memory_write` supersedes parameter
- FR-36b: Embedding queue bounded capacity (10K)
- FR-36c: Queue rejection at 50K threshold (HTTP 503)
- FR-36d: Queue status fields in GET /api/status
- FR-36e: Structured log warnings at 60%/90% capacity
- FR-93b: POST /api/reload-config for hot config reload

### Assumption A-4 updated:
- Old: "Requests without a workspace use the server's default workspace"
- New: "Requests without a workspace return HTTP 400. No default workspace fallback."
