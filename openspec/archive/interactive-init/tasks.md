# Tasks

- [ ] Create `cmd/nano-brain/init.go` with `runInteractiveInit`
- [ ] Modify `runInitCmd` in commands.go to detect interactive mode
- [ ] Wire configPath through from main.go
- [ ] Build and test
